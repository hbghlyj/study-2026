# 2.3
Construct a splitting field for $X^5-2$ over $\mathbb{Q}$. What is its degree over $\mathbb{Q}$?

**Proof**
Let $\zeta$ be a primitive 5th root of unity.
The polynomial $f(X) = X^5-2$ is irreducible over $\mathbb{Q}$ by Eisenstein's criterion with $p=2$, as $2$ divides all non-leading coefficients and $2^2$ does not divide the constant term. Thus, $[\mathbb{Q}(\sqrt[5]{2}):\mathbb{Q}]=5$.
The cyclotomic polynomial $\Phi_5(X) = X^4+X^3+X^2+X+1$ is irreducible. Consider $g(X) = \Phi_5(X+1) = \frac{(X+1)^5-1}{(X+1)-1} = X^4 + 5X^3 + 10X^2 + 10X + 5$. Since $5$ divides $5, 10, 10, 5$ and $5^2 \nmid 5$, $g(X)$ is irreducible by Eisenstein's criterion with $p=5$, implying $\Phi_5(X)$ is irreducible. Thus, $[\mathbb{Q}(\zeta):\mathbb{Q}]=4$.
The splitting field is $K = \mathbb{Q}(\sqrt[5]{2}, \zeta)$. Since $[\mathbb{Q}(\sqrt[5]{2}):\mathbb{Q}]=5$ and $[\mathbb{Q}(\zeta):\mathbb{Q}]=4$ are coprime, the degree of the composite field is $[K:\mathbb{Q}] = 5 \times 4 = 20$.
# 2.6
Let $f(X)$ be an irreducible polynomial in $F[X]$, where $F$ has characteristic $p$. Show that $f(X)$ can be written $f(X)=g(X^{p^e})$ where $g(X)$ is irreducible and separable.
Deduce that every root of $f(X)$ has the same multiplicity $p^e$ in any splitting field.

**Proof**
If $f(X)$ is separable, we take $e=0$ and $g(X)=f(X)$. If $f(X)$ is not separable, then its derivative $f'(X)$ must be the zero polynomial because $f$ is irreducible and $\deg(f') < \deg(f)$. This implies $f(X) = f_1(X^p)$ for some $f_1 \in F[X]$. Since $f$ is irreducible, $f_1$ is also irreducible. By induction, $f(X) = g(X^{p^e})$ for some $e \geq 0$ where $g'(X) \neq 0$. Since $g$ is irreducible and $g'(X) \neq 0$, $g$ is separable. In a splitting field, if $\alpha$ is a root of $g$, then $X^{p^e} - \alpha = (X - \beta)^{p^e}$ where $\beta^{p^e} = \alpha$. Since $g$ is separable, its roots are distinct, so each root of $f$ has multiplicity exactly $p^e$.
# Problem A
Let $E \supset F$ be an algebraic extension. Choose an algebraic closure $\bar{F}$ of $F$ such that $E \subset \bar{F}$. Prove that the following conditions are equivalent:
1. $E$ is the splitting field of a family of polynomials $`S \subset F[x] \setminus \{0\}`$;
2. For any $F$-homomorphism $\phi: E \rightarrow \bar{F}, \phi(E) \subset E$;
3. The minimal polynomial of every $a \in E$ over $F$ splits over $E$.
(The trick to showing that 2 implies 3 is to consider $E$ as an extension of $F(a)$.)

**Proof**
($1 \implies 2$) Since $E$ is the splitting field of a family of polynomials $S \subset F[x] \setminus \{0\}$, $E$ is generated over $F$ by the set $R$ of all roots of polynomials in $S$. An $F$-homomorphism $\phi: E \to \bar{F}$ maps roots of a polynomial $f \in F[x]$ to roots of the same polynomial. Thus $\phi(R) \subseteq R$, which implies $\phi(E) = \phi(F(R)) = F(\phi(R)) \subseteq F(R) = E$.

($2 \implies 3$) Let $a \in E$ and let $b \in \bar{F}$ be a root of the minimal polynomial $m_a \in F[x]$. There exists an $F$-homomorphism $\sigma: F(a) \to \bar{F}$ sending $a$ to $b$. Since $E/F$ is algebraic, $\sigma$ extends to an $F$-homomorphism $\phi: E \to \bar{F}$. By (2), $\phi(E) \subseteq E$, so $b = \phi(a) \in E$. Thus $m_a$ splits in $E$.

($3 \implies 1$) Let $`S = \{m_a \in F[x] : a \in E\}`$ be the set of minimal polynomials of all elements in $E$. By (3), each $f \in S$ splits in $E$. Since $E$ is generated over $F$ by its elements, $E$ is the splitting field of the family $S$.
# Problem B
Let $F$ be a perfect field. Prove that all finite extensions $E \supset F$ are perfect as well.
From this, it easily follows that all algebraic extensions of $F$ are perfect, but you do not need to prove this.

Hint: the most direct way to prove this is to consider the map $x \mapsto x^p: E \rightarrow E$ as a map between $F$-vector spaces, where $p>0$ is the characteristic.
This is not going to work if you consider $E$ as a vector space over $F$ in the obvious way (why?), so you'll have to be clever.

**Proof**

Since $F$ is a perfect field, the Frobenius map $\Phi: F \to F$ given by $x \mapsto x^p$ is an automorphism. Let $E$ be a finite extension of $F$ with $[E:F]=n$. The Frobenius map $\Phi: E \to E$ is an injective field homomorphism. To show $E$ is perfect, we must show $\Phi$ is surjective.

Consider $E$ as an $F$-vector space in two ways: let $V$ be $E$ with the standard action, and let $W$ be $E$ with the action $c \cdot \alpha = c^p \alpha$ for $c \in F, \alpha \in E$. Since $c \mapsto c^p$ is an automorphism of $F$, $W$ is also an $F$-vector space of dimension $n$. The map $\Phi: V \to W$ is $F$-linear because $\Phi(c\alpha) = (c\alpha)^p = c^p \alpha^p = c \cdot \Phi(\alpha)$. Since $\Phi$ is an injective $F$-linear map between finite-dimensional vector spaces of the same dimension, it is surjective. Thus $E^p = E$, so $E$ is perfect.

# Problem C
Let $K$ be the splitting field of the polynomial

$x^4-x^2-1$

over $\mathbb{Q}$. Compute the Galois group of the extension $K / \mathbb{Q}$. (For partial credit, find the degree $[K: \mathbb{Q}]$.)

**Proof**

Let $y_1, y_2$ be the roots of $y^2 - y - 1$, so $y_{1,2} = \frac{1 \pm \sqrt{5}}{2}$. The roots of $f(x) = x^4 - x^2 - 1$ are $\pm \sqrt{y_1}$ and $\pm \sqrt{y_2}$. Let $\alpha = \sqrt{y_1}$. Since $y_1 y_2 = -1$, we have $\sqrt{y_1}\sqrt{y_2} = \pm i$, which implies the splitting field is $K = \mathbb{Q}(\alpha, i)$.

To find $[K : \mathbb{Q}]$, we first show $f(x)$ is irreducible over $\mathbb{Q}$. By the Rational Root Theorem, $f(x)$ has no rational roots. If $f(x) = (x^2 + ax + b)(x^2 + cx + d)$, then $a+c=0, ac+b+d=-1, ad+bc=0, bd=-1$. This implies $a(d-b)=0$. If $a=0$, then $b, d$ are roots of $z^2 + z - 1$, which are not rational. If $d=b$, then $b^2 = -1$, impossible in $\mathbb{Q}$. Thus $f(x)$ is irreducible, so $[\mathbb{Q}(\alpha) : \mathbb{Q}] = 4$. Since $\mathbb{Q}(\alpha) \subset \mathbb{R}$ and $i \notin \mathbb{R}$, $[K : \mathbb{Q}(\alpha)] = 2$, so $[K : \mathbb{Q}] = 8$.

The Galois group $G$ has order 8. It is determined by the maps $\sigma: \alpha \mapsto i/\alpha, \, i \mapsto i$ and $\tau: \alpha \mapsto \alpha, \, i \mapsto -i$. These generators satisfy the relations of $D_8$: $\sigma^2 = \tau^2 = (\sigma\tau)^4 = \mathrm{id}.$

# Problem D
Consider the field of rational functions $E=\mathbb{C}(x)$ and the subfield $F=\mathbb{C}(x^n) \subset E$($n \geq 1$).
Show that $E \supset F$ is a finite Galois extension and find its Galois group.

**Proof**
The extension $E \supset F$ is generated by $x$, which satisfies the polynomial $f(X) = X^n - x^n \in F[X]$. Since $f(X)$ is irreducible over $F$ by Eisenstein's criterion (applied to the prime $x^n \in \mathbb{C}[x^n]$), the degree of the extension is $[E:F] = n$. The splitting field of $f(X)$ is $E$ itself, so $E \supset F$ is a Galois extension. The Galois group $G = \mathrm{Gal}(E/F)$ is generated by the automorphism $\sigma: x \mapsto \zeta_n x$, where $\zeta_n$ is a primitive $n$-th root of unity. Since $\sigma^n(x) = x$, the order of $\sigma$ is $n$, and thus $G \cong C_n$, the cyclic group of order $n$.

# Problem E
Similar to the last problem, suppose $E=\mathbb{C}(x)$, and put

$F=\mathbb{C}(x^n+x^{-n}) \subset E .$

Show that $E \supset F$ is a finite Galois extension and find its Galois group.

**Proof**

The extension $E \supset F$ has degree $2n$. Consider the intermediate field $K = \mathbb{C}(x^n)$. From Problem D, $[E:K] = n$. The element $x^n$ satisfies the quadratic $f(X) = X^2 - (x^n + x^{-n})X + 1 \in F[X]$, which is irreducible over $F$, so $[K:F] = 2$. Thus $[E:F] = 2n$. The extension is Galois as it is the splitting field of $P(X) = X^{2n} - (x^n + x^{-n})X^n + 1 \in F[X]$. The Galois group $G = \mathrm{Gal}(E/F)$ is generated by $\sigma: x \mapsto \zeta_n x$ (where $\zeta_n$ is a primitive $n$-th root of unity) and $\tau: x \mapsto x^{-1}$. These satisfy $\sigma^n = \tau^2 = 1$ and $\tau \sigma \tau = \sigma^{-1}$, so $G \cong D_{2n}$, the dihedral group of order $2n$.
