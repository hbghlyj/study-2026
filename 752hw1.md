1. Show that the functor $\mathrm{Ext}(-,-)$ is contravariant in the first variable, that is, if $H, H'$ and $G$ are abelian groups, a homomorphism $\alpha: H \rightarrow H'$ induces a homomorphism $\alpha^*: \mathrm{Ext}\left(H', G\right) \rightarrow \mathrm{Ext}(H, G)$.
   
   **Proof**

   The contravariant behavior in the first argument is constructed explicitly using projective resolutions. To define the induced map $\alpha^*: \mathrm{Ext}^n(H', G) \to \mathrm{Ext}^n(H, G)$ for a homomorphism $\alpha: H \to H'$:
   
   Construct projective resolutions $P_{\bullet}$ for $H$ and $P_{\bullet}'$ for $H'$. By the Comparison Theorem for resolutions, the morphism $\alpha: H \to H'$ lifts to a chain map $\alpha_{\bullet}: P_{\bullet} \to P_{\bullet}'$ that is unique up to chain homotopy:
   
   $`\begin{CD}
\dots @>>> P_n @>d_n>> P_{n-1} @>>> \dots @>>> P_0 @>\epsilon>> H @>>> 0 \\
@. @V\alpha_nVV @V\alpha_{n-1}VV @. @V\alpha_0VV @V\alpha VV @. \\
\dots @>>> P_n' @>d_n'>> P_{n-1}' @>>> \dots @>>> P_0' @>\epsilon'>> H' @>>> 0
\end{CD}`$
   
   Drop the augmented $H$ term. Apply the contravariant functor $\mathrm{Hom}(-, G)$ to the resolutions. Because $\mathrm{Hom}$ is contravariant in the first slot, the direction of all arrows—including the lifted maps $\alpha_n$—is reversed: $\mathrm{Hom}(\alpha_n, G): \mathrm{Hom}(P_n', G) \rightarrow \mathrm{Hom}(P_n, G)$. This induces a morphism of cochain complexes $`\alpha^\#: \mathrm{Hom}(P_{\bullet}', G) \rightarrow \mathrm{Hom}(P_{\bullet}, G)`$.

   $`\begin{CD}
\mathrm{Hom}(P_0, G) @>\delta^0>> \mathrm{Hom}(P_1, G) @>\delta^1>> \mathrm{Hom}(P_2, G) @>>> \dots \\
@V\mathrm{Hom}(\alpha_0, G)VV @V\mathrm{Hom}(\alpha_1, G)VV @V\mathrm{Hom}(\alpha_2, G)VV \\
\mathrm{Hom}(P_0', G) @>\delta'^0>> \mathrm{Hom}(P_1', G) @>\delta'^1>> \mathrm{Hom}(P_2', G) @>>> \dots
\end{CD}`$
   
   Passing to cohomology, the induced map on the $n$-th cohomology group is $\alpha^*([g]) = [g \circ \alpha_n]$, where $[g] \in \mathrm{Ext}^n(H', G)$ is the cohomology class of a cocycle $g: P_n' \to G$.

   This assignment is well-defined on cohomology classes. The induced cochain map preserves cocycles and coboundaries: since $f_\bullet$ is a chain map,
   
   $`f_n \circ d_{n+1} = d'_{n+1} \circ f_{n+1}`$, so if $g \circ d'_{n+1} = 0$, then
   
   $`(g \circ f_n) \circ d_{n+1} = g \circ d'_{n+1} \circ f_{n+1} = 0`$, making $g \circ f_n$ a cocycle. Similarly, the map sends coboundaries to coboundaries because it commutes with the differentials.
   
   Moreover, the map is independent of the choice of lift $f_\bullet$. By the Comparison Theorem, any two lifts $f_\bullet$ and $h_\bullet$ are chain homotopic, so there exist maps $s_n: P_n \to P'_{n+1}$ such that $`f_n - h_n = d'_{n+1} s_n + s_{n-1} d_n`$. For a cocycle $g$,
   
   $`(g \circ f_n) - (g \circ h_n) = g \circ (d'_{n+1} s_n + s_{n-1} d_n) = (g \circ d'_{n+1}) s_n + (g \circ s_{n-1}) \circ d_n = 0 + \delta^{n-1}(g \circ s_{n-1}),`$
   
   which is a coboundary. Thus, $g \circ f_n$ and $g \circ h_n$ define the same cohomology class.

2. Regarding $`\mathbb{Z}_2`$ as a module over the ring $`\mathbb{Z}_4`$, construct a resolution of $`\mathbb{Z}_2`$ by free modules over $`\mathbb{Z}_4`$, and use this to show that $\mathrm{Ext}_{\mathbb{Z}_4}^n(\mathbb{Z}_2, \mathbb{Z}_2)$ is nonzero for all $n$.
   
   **Proof**

   Using the projective resolution
   
   $\dots\xrightarrow{\times2}\mathbb{Z}_4\xrightarrow{\times2}\mathbb{Z}_4\xrightarrow{\bmod 2}\mathbb{Z}_2\to 0$

   Verify exactness at each position.
   
   - At $\mathbb{Z}_2$, the map $\bmod 2$ is surjective by construction, so exactness holds there.
   
   - For $n \geq 1$, consider exactness at the term $\mathbb{Z}_4$ in position $n$. The outgoing differential is $d_n \colon \mathbb{Z}_4 \to \mathbb{Z}_4$, multiplication by 2.
      
      Its kernel is $`\ker(d_n) = \{ x \in \mathbb{Z}_4 \mid 2x = 0 \} = 2\mathbb{Z}_4.`$
      
      The incoming differential is also multiplication by 2, so its image is
      
      $\mathrm{im}(d_{n+1}) = 2\mathbb{Z}_4.$
      
      Thus, $\ker(d_n) = \mathrm{im}(d_{n+1})$ for all $n \geq 1$, establishing exactness at every positive degree.

   - At the term in position 0 ($\mathbb{Z}_4$), the kernel of the augmentation is $`\{ x \in \mathbb{Z}_4 \mid \pi(x) = 0 \in \mathbb{Z}_2 \} = 2\mathbb{Z}_4 = \mathrm{im}(d_1)`$, so the complex is exact at position 0.

   The group $\mathrm{Hom}_{\mathbb{Z}_4}(\mathbb{Z}_4, \mathbb{Z}_2)$ consists of the zero map and the non-trivial map sending odd multiples of $1$ to $1$ and even multiples to $0$ (the natural quotient map). Addition of homomorphisms corresponds to addition in $`\mathbb{Z}_2`$, so $`\mathrm{Hom}_{\mathbb{Z}_4}(\mathbb{Z}_4, \mathbb{Z}_2) \cong \mathbb{Z}_2`$ via the isomorphism $f \mapsto f(1)$.
   
   The maps in the projective resolution of $\mathbb{Z}_2$ over $\mathbb{Z}_4$ are multiplication by 2, $d_i: \mathbb{Z}_4 \to \mathbb{Z}_4$, $x \mapsto 2x$, for each degree $i \geq 1$.
   
   Applying the functor $\mathrm{Hom}_{\mathbb{Z}_4}(-, \mathbb{Z}_2)$ to the projective resolution of $\mathbb{Z}_2$, yielding groups isomorphic to $\mathbb{Z}_2$ in each non-negative degree with differentials induced by precomposition with multiplication by 2. Any $\mathbb{Z}_4$-homomorphism $f: \mathbb{Z}_4 \to \mathbb{Z}_2$ satisfies $f(2x) = 2 \cdot f(x)$. Since the module structure on $\mathbb{Z}_2$ makes the action of 2 trivial (i.e., $2 \cdot y = 0$ for all $y \in \mathbb{Z}_2$), it follows that $2 \cdot f(x) = 0$. Thus, $f \circ (\times 2) = 0$ for every such $f$.

   All differentials therefore vanish, resulting in the cochain complex
$\dots \leftarrow \mathbb{Z}_2 \xleftarrow{\ 0\ } \mathbb{Z}_2 \xleftarrow{\ 0\ } \mathbb{Z}_2 \xleftarrow{\ 0\ } \mathbb{Z}_2 \leftarrow \dots$
with zero maps in every degree.

   Computing homology
   
   $\mathrm{Ext}_{\mathbb{Z}_4}^n(\mathbb{Z}_2, \mathbb{Z}_2) = \ker(0)/\mathrm{im}(0) \cong \mathbb{Z}_2$ for all $n \ge 0$, which is nonzero.
   
3. Compute the cohomology of the spaces $S^1 \times S^1, \mathbb{R} \mathbb{P}^2$ and the Klein bottle first with $\mathbb{Z}$ coefficients, then with $\mathbb{Z}_2$ coefficients.

   **Proof**

   Using the cellular cohomology approach:

   - $S^1 \times S^1$:
     - CW structure: one 0-cell, two 1-cells, one 2-cell.
     - Cellular cochain complex with $\mathbb{Z}$ coefficients:
       
       $0 \rightarrow \mathbb{Z} \xrightarrow{0} \mathbb{Z}^2 \xrightarrow{0} \mathbb{Z} \rightarrow 0$
       
       Cohomology groups:
       - $H^0(S^1 \times S^1; \mathbb{Z}) \cong \mathbb{Z}$
       - $H^1(S^1 \times S^1; \mathbb{Z}) \cong \mathbb{Z}^2$
       - $H^2(S^1 \times S^1; \mathbb{Z}) \cong \mathbb{Z}$
       
     - With $\mathbb{Z}_2$ coefficients, the same structure applies:
       
       $0 \rightarrow \mathbb{Z}_2 \xrightarrow{0} \mathbb{Z}_2^2 \xrightarrow{0} \mathbb{Z}_2 \rightarrow 0$
       
       Cohomology groups:
       - $H^0(S^1 \times S^1; \mathbb{Z}_2) \cong \mathbb{Z}_2$
       - $H^1(S^1 \times S^1; \mathbb{Z}_2) \cong \mathbb{Z}_2^2$
       - $H^2(S^1 \times S^1; \mathbb{Z}_2) \cong \mathbb{Z}_2$
   - $\mathbb{RP}^2$:
      - CW structure: one 0-cell, one 1-cell, one 2-cell.
      - Cellular cochain complex with $\mathbb{Z}$ coefficients:
         
         $0 \rightarrow \mathbb{Z} \xrightarrow{0} \mathbb{Z} \xrightarrow{2} \mathbb{Z} \rightarrow 0$
         
         Cohomology groups:
         - $H^0(\mathbb{RP}^2; \mathbb{Z}) \cong \mathbb{Z}$
         - $H^1(\mathbb{RP}^2; \mathbb{Z}) \cong 0$
         - $H^2(\mathbb{RP}^2; \mathbb{Z}) \cong \mathbb{Z}_2$
         
      - With $\mathbb{Z}_2$ coefficients:
         
         $0 \rightarrow \mathbb{Z}_2 \xrightarrow{0} \mathbb{Z}_2 \xrightarrow{0} \mathbb{Z}_2 \rightarrow 0$
         
         Cohomology groups:
         - $H^0(\mathbb{RP}^2; \mathbb{Z}_2) \cong \mathbb{Z}_2$
         - $H^1(\mathbb{RP}^2; \mathbb{Z}_2) \cong \mathbb{Z}_2$
         - $H^2(\mathbb{RP}^2; \mathbb{Z}_2) \cong \mathbb{Z}_2$
   - Klein bottle:
      - CW structure: one 0-cell, two 1-cells, one 2-cell.
      - Cellular cochain complex with $\mathbb{Z}$ coefficients:
         
         $0 \rightarrow \mathbb{Z} \xrightarrow{0} \mathbb{Z}^2 \xrightarrow{\begin{pmatrix}2 & 0 \end{pmatrix}} \mathbb{Z} \rightarrow 0$
         
         Cohomology groups:
         - $H^0(K; \mathbb{Z}) \cong \mathbb{Z}$
         - $H^1(K; \mathbb{Z}) \cong \mathbb{Z}$
         - $H^2(K; \mathbb{Z}) \cong \mathbb{Z}_2$

      - With $\mathbb{Z}_2$ coefficients:
         
         $0 \rightarrow \mathbb{Z}_2 \xrightarrow{0} \mathbb{Z}_2^2 \xrightarrow{0} \mathbb{Z}_2 \rightarrow 0$
         
         Cohomology groups:
         - $H^0(K; \mathbb{Z}_2) \cong \mathbb{Z}_2$
         - $H^1(K; \mathbb{Z}_2) \cong \mathbb{Z}_2^2$
         - $H^2(K; \mathbb{Z}_2) \cong \mathbb{Z}_2$

4. Show that if $f: S^n \rightarrow S^n$ has degree $d$, then $f^*: H^n(S^n ; G) \rightarrow H^n(S^n ; G)$ is multiplication by $d$.
   
   **Proof**

   By definition, the degree $d$ of a map $f:S^{n}\rightarrow S^{n}$ is the integer such that the induced map on the $n$-th singular homology group with integer coefficients, $f_{*}:H_{n}(S^{n};\mathbb{Z})\rightarrow H_{n}(S^{n};\mathbb{Z})$, satisfies:
   
   $f_{*}(\alpha )=d\alpha$
   
   where $\alpha$ is a generator of $H_{n}(S^{n};\mathbb{Z})\cong \mathbb{Z}$.

   Using the Universal Coefficient Theorem for cohomology, the fact that $H_{n-1}(S^n)$ is trivial gives a natural isomorphism $H^n(S^n; G) \cong \text{Hom}(H_n(S^n), G)$.

   By naturality, $`f^*`$ corresponds to pre-composition with $f_*$. Let $\hat{\phi} \in \text{Hom}(H_n(S^n), G)$ be the homomorphism corresponding to some $[\phi] \in H^n(S^n; G)$. Let $\alpha$ be a generator for $H_n(S^n) \cong \mathbb{Z}$.

   By definition of degree, we have $f_*(\alpha) = d\alpha$.

   The new homomorphism corresponding to $`f^*([\phi])`$ is $\hat{\phi} \circ f_*$. Evaluating this on the generator $\alpha$:
   
   $`(\hat{\phi} \circ f_*)(\alpha) = \hat{\phi}(f_*(\alpha)) = \hat{\phi}(d\alpha) = d \cdot \hat{\phi}(\alpha)`$.

   Since a homomorphism from $\mathbb{Z}$ is determined by its value on a generator, this shows the new homomorphism is $d$ times the original. Thus, $f^*$ is multiplication by $d$.
5. Show that if $A$ is a closed subspace of $X$ that is a deformation retract of some neighborhood, then the quotient map $X \rightarrow X / A$ induces isomorphisms
    
    $H^n(X, A ; G) \cong \tilde{H}^n(X / A ; G)$
    
    for all $n$.

   **Proof**

   Since $(X,A)$ is a good pair, the quotient map $q: X \to X/A$ induces isomorphisms on integer homology $q_*: H_k(X,A) \xrightarrow{\cong} \tilde{H}_k(X/A)$ for all $k$.

   The Universal Coefficient Theorem for cohomology is natural with respect to $q$, giving a commutative diagram between the UCT short exact sequences for $(X,A)$ and $X/A$.
   
   Since $`q_k: H_k(X,A) \to \tilde{H}_k(X/A)`$ is an isomorphism for all $k$, the induced maps $\text{Hom}(q_k, G)$ and $\text{Ext}(q_{k-1}, G)$ are also isomorphisms. By the Five Lemma, the map induced on cohomology,
   
   $q^*: \tilde{H}^n(X/A;G) \to H^n(X,A;G)$
   
   is also an isomorphism for all $n$.

6. Let $X$ be a space obtained from $S^n$ by attaching a cell $e^{n+1}$ by a degree $m$ map.
    - Show that the quotient map $X \rightarrow X / S^n=S^{n+1}$ induces the trivial map on $\tilde{H}_k(-; \mathbb{Z})$ for all $k$, but not on $H^{n+1}(-; \mathbb{Z})$. Conclude that the splitting in the universal coefficient theorem for cohomology cannot be natural.
    - Show that the inclusion $S^n \hookrightarrow X$ induces the trivial map on $\tilde{H}^i(-; \mathbb{Z})$ for all $i$, but not on $H_n(-; \mathbb{Z})$.
   
   **Proof**
   
   - Consider the quotient map $q: X \to X/S^n \cong S^{n+1}$. For $k \neq n+1$, the long exact sequence in homology for the pair $(X, S^n)$ gives:
      
      $`\dots H_k(S^n) \xrightarrow{i_*} H_k(X) \xrightarrow{q_*} H_k(X, S^n)\cdots`$
      
      $H_k(X,S^n)$ is trivial. This implies the map $`i_*: \tilde{H}_k(S^n) \to \tilde{H}_k(X)`$ is surjective. Since $`q_*`$ vanishes on the image of $i_*$, it follows that $`q_*: \tilde{H}_k(X) \to \tilde{H}_k(S^{n+1})`$ is trivial.
      
      For $k=n+1$, the long exact sequence in homology for the pair $(X, S^n)$ gives:
      
      $\dots \to H_{n+1}(X) \xrightarrow{q_*} H_{n+1}(X, S^n) \xrightarrow{\partial} H_n(S^n) \to \cdots$
      
      The boundary map $\partial$ is multiplication by $m$. Assuming $m \neq 0$, $\partial$ is injective, so $\mathrm{im}(q_*) = \ker(\partial) = 0$. Thus $`q_*`$ is the zero map, and $`q_*`$ is trivial for all $i$.
      
      Now consider cohomology. The long exact sequence in cohomology for the pair $(X, S^n)$ gives:

      $`\dots \to H^{n+1}(X, S^n) \xrightarrow{q^*} H^{n+1}(X) \xrightarrow{i^*} H^{n+1}(S^n) \to \cdots`$

      Here, $H^{n+1}(S^n) = 0$, so $`i^*`$ is the zero map, making $`q^*`$ surjective. Since $H^{n+1}(X, S^n) \cong \mathbb{Z}$ and $H^{n+1}(X) \cong \mathbb{Z}/m\mathbb{Z}$, for $m \neq \pm 1$, $H^{n+1}(X)$ is non-trivial, so $`q^*`$ is non-trivial.

      Since the quotient map $q$ induces the zero map on homology, any natural splitting of the Universal Coefficient Theorem would force the induced map $q^*$ on cohomology to be zero (as the maps on Hom and Ext terms would vanish), yet $`q^*`$ is non-trivial on $H^{n+1}(-; \mathbb{Z})$ as shown above, leading to a contradiction.

7. For a topological space $X$, let
        
    $\langle,\rangle: C^n(X) \otimes C_n(X) \rightarrow \mathbb{Z}$
    
    be the Kronecker pairing given by $\langle\phi, \sigma\rangle:=\phi(\sigma)$. In terms of this pairing, the coboundary map $\delta: C^n(X) \rightarrow C^{n+1}(X)$ is defined by $\langle\delta(\phi), \sigma\rangle=\langle\phi, \partial \sigma\rangle$ for all $\sigma \in C_{n+1}(X)$. Show that this pairing induces a pairing between cohomology and homology:
    
    $\langle,\rangle: H^n(X ; \mathbb{Z}) \otimes H_n(X ; \mathbb{Z}) \rightarrow \mathbb{Z} .$

    **Proof**

    Define $\langle,\rangle: H^n(X ; \mathbb{Z}) \times H_n(X ; \mathbb{Z}) \rightarrow \mathbb{Z}$ by $\langle [\phi], [\sigma] \rangle = \phi(\sigma)$.
    This map is well-defined, and independent of the choice of representatives:
     - If $[\phi]=0$, then $\phi = \delta\psi$. For any cycle $\sigma$, $\langle [\phi], [\sigma] \rangle = \phi(\sigma) = (\delta\psi)(\sigma) = \psi(\partial\sigma) = \psi(0) = 0$.
     - If $[\sigma]=0$, then $\sigma = \partial\tau$. For any cocycle $\phi$, $\langle [\phi], [\sigma] \rangle = \phi(\sigma) = \phi(\partial\tau) = (\delta\phi)(\tau) = 0(\tau) = 0$.
    
    This map is also bilinear, so it induces a map $\langle,\rangle: H^n(X ; \mathbb{Z}) \otimes H_n(X ; \mathbb{Z}) \rightarrow \mathbb{Z}$.
   
    Degeneracy:
    Any element $\alpha \in H_{n}(X ; \mathbb{Z})$ of finite order $k$ (where $k\alpha=0$) must pair with any cohomology class $\varphi$ to yield $0$, since $k\langle \varphi, \alpha \rangle =\langle \varphi, k\alpha \rangle =0$ in $\mathbb{Z}$, which is torsion-free.
