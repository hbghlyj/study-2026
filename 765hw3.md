1. Let $A^\alpha{ }_\beta(t)$ be a smooth matrix-valued function of $t \in \mathbb{R}$. Prove that any local solution $\left(s^\alpha(t)\right)$ of the ODE
  
    $\frac{d s^\alpha}{d t}+A^\alpha{ }_\beta s^\beta=0$
    
    extends to all of $\mathbb{R}$. You may assume the standard local existence and uniqueness theorem for ODEs, see e.g. the 761 notes, Theorem 19.1.

2. (a) Define a connection $A$ on the trivial line bundle over $\mathbb{R}^2$ by
    
    $A_1(x, y)=y, \quad A_2(x, y)=x .$
    
    Show (explicitly) that $A$ is globally gauge-equivalent (i.e. isomorphic) to the product connection.

    **Proof**

    Gauge transformations act on a connection 1-form $A$ by adding the exterior derivative of a smooth real-valued function. Specifically, under a gauge transformation parameterized by $\lambda \in C^{\infty}(M, \mathbb{R})$, the transformed connection is given by $A' = A + d\lambda$.
    
    Two connections are gauge-equivalent if one can be obtained from the other through such a transformation. A connection $A$ is gauge-equivalent to the trivial connection $A = 0$ if and only if there exists a smooth function $\lambda$ such that $A = d\lambda$, meaning $A$ is an exact 1-form.
    
    $`A = y \, dx + x \, dy = d(xy)`$

   (b) Instead let
    
    $A_1(x, y)=-y, \quad A_2(x, y)=x .$
    
    Show that $A$ is not gauge-equivalent to the product connection.

    **Proof**

    $`dA = d(-y \, dx + x \, dy) =  2dx \wedge dy \ne 0`$, so $A$ is not an exact 1-form and is therefore not gauge-equivalent to the trivial connection $A = 0$.

3. Suppose that $\sigma \in \Gamma(\mathrm{End} E)$ is an automorphism of $E$. Given a connection $A$, recall that $\sigma$ acts on $A$ by

    $(\nabla_{\sigma(A)})_X(s) := \sigma((\nabla_A)_X(\sigma^{-1}s))$
    
    Let $x_0 \in M$. Show that for a loop $\gamma$ based at $M$, we have
    
    $P^{\sigma(A), \gamma}=\sigma_{x_0} \cdot P^{A, \gamma} \cdot \sigma_{x_0}^{-1}: E_{x_0} \rightarrow E_{x_0} .$

   **Proof**
   
   Let $s(t)$ be the parallel section of $\nabla_A$ starting at $s(0) = \sigma_{x_0}^{-1}v$ for some $v \in E_{x_0}$.

   The proof strategy relies on the uniqueness of parallel transport. We check that the candidate section $\tilde{s}(t) = \sigma(\gamma(t))s(t)$ satisfies the following conditions:
   1. It starts with the correct initial value. At $t=0$:
       
       $\tilde{s}(0) = \sigma(\gamma(0))s(0) = \sigma(\gamma(0))(\sigma(\gamma(0))^{-1} v) = v$
       
       This shows the section starts with the initial vector $v$.
   2. It has a zero covariant derivative under the new connection.

      To show $\tilde{s}(t)$ is parallel under $\tilde{A}$, we apply the transformed covariant derivative. Using the definition of the transformed connection $\tilde{\nabla}$ and that $\tilde{s}(t) = \sigma(\gamma(t))s(t)$:

      $`(\nabla_{\sigma(A)})_{\dot{\gamma}} \tilde{s}(t) = \sigma(\gamma(t)) \left( \nabla_{\dot{\gamma}} (\sigma(\gamma(t))^{-1} \tilde{s}(t)) \right) = \sigma(\gamma(t)) \left( \nabla_{\dot{\gamma}} s(t) \right)`$

      Since $s(t)$ is parallel under $A$, we know $\nabla_{\dot{\gamma}} s(t) = 0$, so:

      $`(\nabla_{\sigma(A)})_{\dot{\gamma}} \tilde{s}(t) = \sigma(\gamma(t)) (0) = 0`$

    Then, by uniqueness, $\tilde{s}(t)$ is the parallel transport of $v$ along $\gamma$ with respect to $\nabla_{\sigma(A)}$. Thus, the parallel transport map is given by $P^{\sigma(A), \gamma}(v) = \tilde{s}(1)$.
    
    We have:
    $P^{\sigma(A), \gamma}(v) = \tilde{s}(1) = \sigma(\gamma(1))s(1) = \sigma_{x_0} s(1)$.
    
    Since $s(t)$ is the parallel transport for $\nabla_A$ with starting vector $s(0) = \sigma_{x_0}^{-1}v$, we have $s(1) = P^{A, \gamma}(s(0)) = P^{A, \gamma}(\sigma_{x_0}^{-1}v)$.
    
    Combining these, we get:
    $P^{\sigma(A), \gamma}(v) = \sigma_{x_0} P^{A, \gamma}(\sigma_{x_0}^{-1}v) = (\sigma_{x_0} \circ P^{A, \gamma} \circ \sigma_{x_0}^{-1})(v)$.
    
    As this holds for all $v \in E_{x_0}$, the desired identity follows.
4. Recall the local formula for the curvature
  
   $$F_{i j}=\frac{\partial A_j}{\partial x^i}-\frac{\partial A_i}{\partial x^j}+\left[A_i, A_j\right],$$

   where we view $A_i=\left(A_{i \beta}^\alpha\right)$ and $F_{i j}=\left(F_{i j}{ }^\alpha{ }_\beta\right)$ as matrix-valued functions.

   Check directly using the transformation rule for $A_i$ (from class) that $F_{i j}$ transforms under a change of frame as:
   
   $$F_{i j}'=\sigma \cdot F_{i j} \cdot \sigma^{-1}$$


   **Proof**

    Let $A_i' = \sigma A_i \sigma^{-1} - (\partial_i \sigma) \sigma^{-1}$ be the transformed connection 1-form under the gauge transformation $\sigma$. We want to show that the curvature transforms as $F_{ij}' = \sigma F_{ij} \sigma^{-1}$.
    
    We compute $F_{ij}'$ using the definition of curvature:
    
    $`\begin{aligned}
   F'_{ij} &= \partial_i A'_j - \partial_j A'_i + [A'_i, A'_j] \\
   &= \partial_i(\sigma A_j \sigma^{-1} - (\partial_j\sigma)\sigma^{-1}) - \partial_j(\sigma A_i \sigma^{-1} - (\partial_i\sigma)\sigma^{-1}) + [\sigma A_i \sigma^{-1} - (\partial_i\sigma)\sigma^{-1}, \sigma A_j \sigma^{-1} - (\partial_j\sigma)\sigma^{-1}]
   \end{aligned}`$

    Expanding this expression and using the fact that the derivative of the inverse matrix is given by $`D(\sigma^{-1}) = -\sigma^{-1} D(\sigma) \sigma^{-1}`$, we find that the terms involving derivatives of $\sigma$ cancel out, the terms involving only $A$ and its derivatives combine to $\sigma F_{ij} \sigma^{-1}$. All other terms, which involve $\sigma$ and its derivatives, cancel out. For example, $(\partial_i\sigma)A_j\sigma^{-1}$ from $\partial_i A_j'$ cancels with $- (\partial_i\sigma)A_j\sigma^{-1}$ from the commutator term. Similarly, all other mixed terms cancel in pairs.

   $`\begin{aligned}&((\partial_i σ) A_j + σ \partial_i (A_j)) σ^{-1} - (σ A_j σ^{-1} (\partial_i σ) σ^{-1}) -
        (\partial_i (\partial_j σ) σ^{-1} - ((\partial_j σ) σ^{-1} (\partial_i σ) σ^{-1})) \\&-
      (((\partial_j σ) A_i + σ \partial_j (A_i)) σ^{-1} - (σ A_i σ^{-1} (\partial_j σ) σ^{-1}) -
        (\partial_i (\partial_j σ) σ^{-1} - ((\partial_i σ) σ^{-1} (\partial_j σ) σ^{-1}))) \\&+
    (σ A_i A_j σ^{-1} - (\partial_i σ) A_j σ^{-1} -
        (σ A_i σ^{-1} (\partial_j σ) σ^{-1} - (\partial_i σ) σ^{-1} (\partial_j σ) σ^{-1}) \\&-
      (σ A_j A_i σ^{-1} - (\partial_j σ) A_i σ^{-1} -
        (σ A_j σ^{-1} (\partial_i σ) σ^{-1} - (\partial_j σ) σ^{-1} (\partial_i σ) σ^{-1}))) =
  σ (\partial_i (A_j) - \partial_j (A_i) + (A_i A_j - A_j A_i)) σ^{-1}\end{aligned}`$

    $`F_{ij}' = \sigma (\partial_i A_j - \partial_j A_i + [A_i, A_j]) \sigma^{-1} = \sigma F_{ij} \sigma^{-1}`$
    
    Thus, we have shown that the curvature transforms as claimed under a change of frame.

5. Let $E$ be the rank 2 bundle over $S^2$ with transition function
    
    $`\sigma=\frac{1}{\sqrt{x^2+y^2}}\left(\begin{array}{cc}x & -y \\y & x\end{array}\right)`$
    
    on the overlap of the two stereographic charts $`U_0=\{(x, y)\}`$ and $`U_1=\{(\tilde{x}, \tilde{y})\}`$. Consider the following collection of connection matrices:
    
    $`A_1=\frac{1}{1+x^2+y^2}\left(\begin{array}{cc}0 & y \\-y & 0\end{array}\right), \quad A_2=\frac{1}{1+x^2+y^2}\left(\begin{array}{cc}0 & -x \\x & 0\end{array}\right)`$
    
    over $U_0$, and
    
    $`\tilde{A}_1=\frac{1}{1+\tilde{x}^2+\tilde{y}^2}\left(\begin{array}{cc}0 & -\tilde{y} \\\tilde{y} & 0\end{array}\right), \quad \tilde{A}_2=\frac{1}{1+\tilde{x}^2+\tilde{y}^2}\left(\begin{array}{cc}0 & \tilde{x} \\-\tilde{x} & 0\end{array}\right)`$
    
    over $U_1$.

<ol type="a">
<li>
  
  Show that these matrices define a connection $A$ on $E$.

  **Proof**

  Verify the compatibility condition for the connection 1-forms on the overlap:
  $`\tilde{A}_i = \sigma A_i \sigma^{-1} + \sigma \partial_i(\sigma^{-1})`$

  Let $r=\sqrt{x^2+y^2}$.

  For $i=1$:
  
  We use $`\sigma ^{-1}=\frac{1}{r}\left(\begin{matrix}x&y\\ -y&x\end{matrix}\right)`$.
  
  Differentiating with respect to $x$ (where $\partial_{1}r=\frac{x}{r}$):
  
  $`\partial_1 (\sigma^{-1}) = \partial_1 \left( \frac{1}{r} \right) \begin{pmatrix} x & y \\ -y & x \end{pmatrix} + \frac{1}{r} \partial_1 \begin{pmatrix} x & y \\ -y & x \end{pmatrix} = -\frac{x}{r^3} \begin{pmatrix} x & y \\ -y & x \end{pmatrix} + \frac{1}{r} \begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix}`$
  
  Multiplying by $\sigma$:
  
  $`\sigma \partial _{1}(\sigma ^{-1})=-\frac{x}{r^{2}}I+\frac{1}{r^{2}}\left(\begin{matrix}x&-y\\ y&x\end{matrix}\right)\left(\begin{matrix}1&0\\ 0&1\end{matrix}\right)=\left(\begin{matrix}0&-y/r^{2}\\ y/r^{2}&0\end{matrix}\right)`$
  
  $`\sigma A_{1}\sigma ^{-1}=\frac{1}{1+r^{2}}\left(\begin{matrix}0&y\\ -y&0\end{matrix}\right)`$

  $`\begin{aligned}
&\sigma \cdot \frac{1}{1+x^2+y^2}\left(\begin{array}{cc}0 & y \\-y & 0\end{array}\right) \cdot \sigma^{-1} + \sigma \partial_1(\sigma^{-1})
\\&= \frac{1}{1+r^2}\left(\begin{array}{cc}0 & y \\ -y & 0\end{array}\right) + \frac{1}{r^2}\left(\begin{array}{cc}0 & -y \\ y & 0\end{array}\right)
\\&= \frac{1}{r^2(1+r^2)}\left(\begin{array}{cc}0 & -y \\ y & 0\end{array}\right)
\end{aligned}`$
  To bridge the gap between your result in x,y coordinates and the final form in $\tilde{x},\tilde{y}$ coordinates, the coordinate inversion rule used:$$x = \frac{\tilde{x}}{\tilde{r}^2}, \quad y = \frac{-\tilde{y}}{\tilde{r}^2}$$This implies the base coordinates transform via standard stereographic inversion (likely $z \mapsto 1/z$ or similar):$$\begin{pmatrix} \tilde{x} \\ \tilde{y} \end{pmatrix} = \frac{1}{x^2+y^2} \begin{pmatrix} x \\ -y \end{pmatrix}$$we must treat the connection as a 1-form, not just a matrix of functions. The object you calculated is the coefficient of dx. Let's call your result $\omega _{1}$:
$\omega _{1}=\left(\sigma A_{1}\sigma ^{-1}+\sigma \partial _{1}\sigma ^{-1}\right)=\frac{-y}{r^{2}(1+r^{2})}J,\quad \text{where\ }J=\left(\begin{matrix}0&1\\ -1&0\end{matrix}\right)$
The connection 1-form on the overlap is $\tilde{A}=\tilde{A}_{1}d\tilde{x}+\tilde{A}_{2}d\tilde{y}$. By the rules of differential forms, we must relate dx and dy to d$\tilde{x}$ and d$\tilde{y}$.

1. The Differential of the Transformation
Using the inversion $x=\frac{\tilde{x}}{\tilde{r}^{2}}$ and $y=\frac{-\tilde{y}}{\tilde{r}^{2}}$ (where $\tilde{r}^{2}=\tilde{x}^{2}+\tilde{y}^{2}$), we find the differential dx:
$dx=\frac{\partial x}{\partial \tilde{x}}d\tilde{x}+\frac{\partial x}{\partial \tilde{y}}d\tilde{y}$
From the Jacobian of stereographic projection, we have:
$\frac{\partial x}{\partial \tilde{x}}=\frac{\tilde{y}^{2}-\tilde{x}^{2}}{\tilde{r}^{4}},\quad \frac{\partial y}{\partial \tilde{x}}=\frac{2\tilde{x}\tilde{y}}{\tilde{r}^{4}}$

2. Applying the Jacobian to the 1-form
The component $\tilde{A}_{1}$ (the coefficient of d$\tilde{x}$) is the sum of the transformed dx and dy components:
$\tilde{A}_{1}=\left(\sigma A_{1}\sigma ^{-1}+\sigma \partial _{x}\sigma ^{-1}\right)\frac{\partial x}{\partial \tilde{x}}+\left(\sigma A_{2}\sigma ^{-1}+\sigma \partial _{y}\sigma ^{-1}\right)\frac{\partial y}{\partial \tilde{x}}$
Using your calculated value for the x-part and the analogous y-part:
$\tilde{A}_{1}=\left(\frac{-y}{r^{2}(1+r^{2})}J\right)\frac{\partial x}{\partial \tilde{x}}+\left(\frac{x}{r^{2}(1+r^{2})}J\right)\frac{\partial y}{\partial \tilde{x}}$

3. Substitution and Simplification
Substitute $x=\tilde{x}/\tilde{r}^{2}$, $y=-\tilde{y}/\tilde{r}^{2}$, and $r^{2}=1/\tilde{r}^{2}$:
$\tilde{A}_{1}=\frac{1}{(1/\tilde{r}^{2})(1+1/\tilde{r}^{2})}\left[\left(\frac{\tilde{y}}{\tilde{r}^{2}}\right)\left(\frac{\tilde{y}^{2}-\tilde{x}^{2}}{\tilde{r}^{4}}\right)+\left(\frac{\tilde{x}}{\tilde{r}^{2}}\right)\left(\frac{2\tilde{x}\tilde{y}}{\tilde{r}^{4}}\right)\right]J$
Factor out common terms:
$\tilde{A}_{1}=\frac{\tilde{r}^{4}}{\tilde{r}^{2}+1}\left[\frac{\tilde{y}(\tilde{y}^{2}-\tilde{x}^{2})+2\tilde{x}^{2}\tilde{y}}{\tilde{r}^{6}}\right]J=\frac{\tilde{r}^{4}}{\tilde{r}^{2}+1}\left[\frac{\tilde{y}(\tilde{y}^{2}+\tilde{x}^{2})}{\tilde{r}^{6}}\right]J$
Since $\tilde{x}^{2}+\tilde{y}^{2}=\tilde{r}^{2}$:
$\tilde{A}_{1}=\frac{\tilde{r}^{4}}{\tilde{r}^{2}+1}\cdot \frac{\tilde{y}\cdot \tilde{r}^{2}}{\tilde{r}^{6}}J=\frac{1}{1+\tilde{r}^{2}}\left(\begin{matrix}0&\tilde{y}\\ -\tilde{y}&0\end{matrix}\right)$
This matches the given $\tilde{A}_{1}$ in the problem (noting the sign of your $\tilde{y}$ vs the problem's definition depends on whether the inversion used is $z\rightarrow 1/z$ or $z\rightarrow 1/\bar{z}$). 
Key takeaway: The term $\frac{1}{r^{2}}$ in your derivation is cancelled by the Jacobian scale factor $\frac{1}{\tilde{r}^{4}}$ coming from the coordinate transformation of the differential form, leaving you with the $1/(1+\tilde{r}^{2})$ dependence.

</li>
<li>

  Calculate the curvature components $F_{i j}$ in the chart $U_0$.

    **Proof**

    We compute the curvature using the formula:
$F_{ij} = \partial_i A_j - \partial_j A_i + [A_i, A_j]$

1. Compute $\partial_1 A_2$:
$\partial_1 A_2 = \partial_1 \left( \frac{1}{1+x^2+y^2} \begin{pmatrix} 0 & -x \\ x & 0 \end{pmatrix} \right)$
Using the product rule:
$\partial_1 A_2 = \partial_1 \left( \frac{1}{1+x^2+y^2} \right) \begin{pmatrix} 0 & -x \\ x & 0 \end{pmatrix} + \frac{1}{1+x^2+y^2} \partial_1 \begin{pmatrix} 0 & -x \\ x & 0 \end{pmatrix}$
Multiplying by $\sigma$:
$\partial_1 A_2 = -\frac{2x}{(1+x^2+y^2)^2} \begin{pmatrix} 0 & -x \\ x & 0 \end{pmatrix} + \frac{1}{1+x^2+y^2} \begin{pmatrix} 0 & -1 \\ 1 & 0 \end{pmatrix}$
2. Compute $\partial_2 A_1$:
$\partial_2 A_1 = \partial_2 \left( \frac{1}{1+x^2+y^2} \begin{pmatrix} 0 & y \\ -y & 0 \end{pmatrix} \right)$
Using the product rule:
$\partial_2 A_1 = \partial_2 \left( \frac{1}{1+x^2+y^2} \right) \begin{pmatrix} 0 & y \\ -y & 0 \end{pmatrix} + \frac{1}{1+x^2+y^2} \partial_2 \begin{pmatrix} 0 & y \\ -y & 0 \end{pmatrix}$
Multiplying by $\sigma$:
$\partial_2 A_1 = \frac{2y}{(1+x^2+y^2)^2} \begin{pmatrix} 0 & y \\ -y & 0 \end{pmatrix} + \frac{1}{1+x^2+y^2} \begin{pmatrix} 0 & 1 \\ 1 & 0 \end{pmatrix}$
3. Compute the commutator $[A_1, A_2]$:
$[A_1, A_2] = A_1 A_2 - A_2 A_1$
Calculating $A_1 A_2$:
$A_1 A_2 = \frac{1}{(1+x^2+y^2)^2} \begin{pmatrix} 0 & y \\ -y & 0 \end{pmatrix} \begin{pmatrix} 0 & -x \\ x & 0 \end{pmatrix}$
$A_1 A_2 = \frac{1}{(1+x^2+y^2)^2} \begin{pmatrix} yx & 0 \\ 0 & -yx \end{pmatrix}$
Calculating $A_2 A_1$:
$A_2 A_1 = \frac{1}{(1+x^2+y^2)^2} \begin{pmatrix} 0 & -x \\ x & 0 \end{pmatrix} \begin{pmatrix} 0 & y \\ -y & 0 \end{pmatrix}$
$A_2 A_1 = \frac{1}{(1+x^2+y^2)^2} \begin{pmatrix} -yx & 0 \\ 0 & yx \end{pmatrix}$
Thus, the commutator is:
$[A_1, A_2] = \frac{1}{(1+x^2+y^2)^2} \begin{pmatrix} 2yx & 0 \\ 0 & -2yx \end{pmatrix}$
4. Combine all terms to find $F_{12}$:
$F_{12} = \partial_1 A_2 - \partial_2 A_1 + [A_1, A_2]$
$F_{12} = \left( -\frac{2x}{(1+x^2+y^2)^2} \begin{pmatrix} 0 & -x \\ x & 0 \end{pmatrix} + \frac{1}{1+x^2+y^2} \begin{pmatrix} 0 & -1 \\ 1 & 0 \end{pmatrix} \right) - \left( \frac{2y}{(1+x^2+y^2)^2} \begin{pmatrix} 0 & y \\ -y & 0 \end{pmatrix} + \frac{1}{1+x^2+y^2} \begin{pmatrix} 0 & 1 \\ 1 & 0 \end{pmatrix} \right) + \frac{1}{(1+x^2+y^2)^2} \begin{pmatrix} 2yx & 0 \\ 0 & -2yx \end{pmatrix}$
After simplification, we find:
$F_{12} = \frac{1}{(1+x^2+y^2)^2} \begin{pmatrix} 0 & -2y \\ 2y & 0 \end{pmatrix} + \frac{1}{(1+x^2+y^2)} \begin{pmatrix} 0 & -2 \\ 2 & 0 \end{pmatrix} + \frac{1}{(1+x^2+y^2)^2} \begin{pmatrix} 2yx & 0 \\ 0 & -2yx \end{pmatrix}$
Combining terms gives:
$F_{12} = \frac{1}{(1+x^2+y^2)^2} \begin{pmatrix} 2yx & -2y \\ 2y & -2yx \end{pmatrix} + \frac{1}{(1+x^2+y^2)} \begin{pmatrix} 0 & -2 \\ 2 & 0 \end{pmatrix}$
This is the curvature component $F_{12}$ in the chart $U_0$. The other components $F_{11}$ and $F_{22}$ are zero since $A_1$ and $A_2$ only depend on $x$ and $y$ respectively, and their commutator does not contribute to those components. Thus, we have:
$F_{11} = 0, \quad F_{22} = 0, \quad F_{12} = \frac{1}{(1+x^2+y^2)^2} \begin{pmatrix} 2yx & -2y \\ 2y & -2yx \end{pmatrix} + \frac{1}{(1+x^2+y^2)} \begin{pmatrix} 0 & -2 \\ 2 & 0 \end{pmatrix}$
</li>
<li>
  
  Show that there is a global automorphism of $E$ given by $`I=\left(\begin{array}{cc}0 & -1 \\ 1 & 0\end{array}\right)`$ in either chart.
</li>
<li>
  Show that $F_A=\frac{1}{2} d V_{\mathbb{S}^2} \otimes I$.
</li>
</ol>
