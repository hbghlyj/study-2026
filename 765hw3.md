1. Let $A^\alpha{ }_\beta(t)$ be a smooth matrix-valued function of $t \in \mathbb{R}$. Prove that any local solution $\left(s^\alpha(t)\right)$ of the ODE
  
    $\frac{d s^\alpha}{d t}+A^\alpha{ }_\beta s^\beta=0$
    
    extends to all of $\mathbb{R}$. You may assume the standard local existence and uniqueness theorem for ODEs, see e.g. the 761 notes, Theorem 19.1.

    **Proof**

    First, the maximal interval of existence $(a, b)$ is considered. On any compact subinterval $[u,v] \subset (a,b)$, continuity of $A$ gives a uniform bound $`M := \sup_{t \in [u,v]} \|A(t)\| < \infty`$. The norm satisfies
    
    $`\frac{d}{dt} \|s(t)\|^2 = -2 \langle s(t), A(t) s(t) \rangle \le 2 \|A(t)\| \|s(t)\|^2 \le 2M \|s(t)\|^2,`$
    
    and by Grönwall's inequality,
    
    $`\|s(t)\| \le \|s(u)\| e^{M |t-u|}`$

    on $[u,v]$, so $s$ is bounded on compact subintervals.
    
    To show that there is no finite-time breakdown, assume toward a contradiction that the right endpoint $b < \infty$. Choose $\delta > 0$ such that $(b - \delta, b) \subset (a, b)$. By compactness of $[b - \delta, b + \delta]$ and the continuity of $A$ on $\mathbb{R}$, we have
    
    $`L := \sup_{t \in [b - \delta, b + \delta]} \|A(t)\| < \infty.`$

    Using the exponential bound from Grönwall's inequality applied on compact subintervals of $[b - \delta, b)$, there exists
    
    $`K := \sup_{t \in [b - \delta, b)} \|s(t)\| < \infty.`$

    Consider the rectangle
    
    $\mathcal{R} := [b - \delta, b + \delta] \times B_{2K} \subset \mathbb{R} \times \mathbb{R}^n,$
    
    where $`B_{2K} = \{y : \|y\| \le 2K\}`$. The vector field $f(t,y) = -A(t)y$ is continuous on $\mathcal{R}$ and Lipschitz in $y$ with constant $L$; moreover, $`\|f(t,y)\| \le L \|y\| \le 2LK`$ on $\mathcal{R}$.

    By the uniform local existence and uniqueness theorem on rectangles (a consequence of the Picard–Lindelöf theorem with uniform estimates), there exists $h > 0$ (depending only on $\delta, L, K$) such that for any $`t_* \in [b - \delta, b + \delta]`$ and any $`y_*`$ with $`\|y_*\| \le K`$, the IVP $`y'(t) = f(t,y(t))$, $y(t_*) = y_*`$, has a unique solution on $`[t_* - h, t_* + h]`$ that remains in $B_{2K}$. In particular, choose $`t_* \in (b - h/2, b)`$ and set $`y_* = s(t_*)`$ (note $`\|y_*\| \le K`$). The corresponding solution extends to $`[t_*, t_* + h]`$ with $`t_* + h > b`$.

    By uniqueness, this solution agrees with the original $s$ on $(t_* - h, b)$, hence it extends $s$ past $b$, contradicting maximality.

    A similar argument, applied to a right neighborhood of $a$ and extending backward in time, shows that $a > -\infty$ also leads to a contradiction.
    
    Thus, $a = -\infty$ and $b = +\infty$.
    
2. (a) Define a connection $A$ on the trivial line bundle over $\mathbb{R}^2$ by
    
    $A_1(x, y)=y, \quad A_2(x, y)=x .$
    
    Show (explicitly) that $A$ is globally gauge-equivalent (i.e. isomorphic) to the product connection.

    **Proof**

    Gauge transformations act on a connection 1-form $A$ by adding the exterior derivative of a smooth real-valued function. Specifically, under a gauge transformation parameterized by $\lambda \in C^{\infty}(M, \mathbb{R})$, the transformed connection is given by $A' = A + d\lambda$.
    
    Two connections are gauge-equivalent if one can be obtained from the other through such a transformation. A connection $A$ is gauge-equivalent to the trivial connection $A = 0$ if and only if there exists a smooth function $\lambda$ such that $A = d\lambda$, meaning $A$ is an exact 1-form.
    
    $`A = y \, dx + x \, dy = d(xy)`$

   (b) Instead let
    
    $A_1(x, y)=-y, \quad A_2(x, y)=x .$
    
    Show that $A$ is not gauge-equivalent to the product connection.

    **Proof**

    $`dA = d(-y \, dx + x \, dy) =  2dx \wedge dy \ne 0`$, so $A$ is not an exact 1-form and is therefore not gauge-equivalent to the trivial connection $A = 0$.

3. Suppose that $\sigma \in \Gamma(\mathrm{End} E)$ is an automorphism of $E$. Given a connection $A$, recall that $\sigma$ acts on $A$ by

    $(\nabla_{\sigma(A)})_X(s) := \sigma((\nabla_A)_X(\sigma^{-1}s))$
    
    Let $x_0 \in M$. Show that for a loop $\gamma$ based at $M$, we have
    
    $P^{\sigma(A), \gamma}=\sigma_{x_0} \cdot P^{A, \gamma} \cdot \sigma_{x_0}^{-1}: E_{x_0} \rightarrow E_{x_0} .$

   **Proof**
   
   Let $s(t)$ be the parallel section of $\nabla_A$ starting at $s(0) = \sigma_{x_0}^{-1}v$ for some $v \in E_{x_0}$.

   The proof strategy relies on the uniqueness of parallel transport. We check that the candidate section $\tilde{s}(t) = \sigma(\gamma(t))s(t)$ satisfies the following conditions:
   1. It starts with the correct initial value. At $t=0$:
       
       $\tilde{s}(0) = \sigma(\gamma(0))s(0) = \sigma(\gamma(0))(\sigma(\gamma(0))^{-1} v) = v$
       
       This shows the section starts with the initial vector $v$.
   2. It has a zero covariant derivative under the new connection.

      To show $\tilde{s}(t)$ is parallel under $\tilde{A}$, we apply the transformed covariant derivative. Using the definition of the transformed connection $\tilde{\nabla}$ and that $\tilde{s}(t) = \sigma(\gamma(t))s(t)$:

      $`(\nabla_{\sigma(A)})_{\dot{\gamma}} \tilde{s}(t) = \sigma(\gamma(t)) \left( \nabla_{\dot{\gamma}} (\sigma(\gamma(t))^{-1} \tilde{s}(t)) \right) = \sigma(\gamma(t)) \left( \nabla_{\dot{\gamma}} s(t) \right)`$

      Since $s(t)$ is parallel under $A$, we know $\nabla_{\dot{\gamma}} s(t) = 0$, so:

      $`(\nabla_{\sigma(A)})_{\dot{\gamma}} \tilde{s}(t) = \sigma(\gamma(t)) (0) = 0`$

    Then, by uniqueness, $\tilde{s}(t)$ is the parallel transport of $v$ along $\gamma$ with respect to $\nabla_{\sigma(A)}$. Thus, the parallel transport map is given by $P^{\sigma(A), \gamma}(v) = \tilde{s}(1)$.
    
    We have:
    $P^{\sigma(A), \gamma}(v) = \tilde{s}(1) = \sigma(\gamma(1))s(1) = \sigma_{x_0} s(1)$.
    
    Since $s(t)$ is the parallel transport for $\nabla_A$ with starting vector $s(0) = \sigma_{x_0}^{-1}v$, we have $s(1) = P^{A, \gamma}(s(0)) = P^{A, \gamma}(\sigma_{x_0}^{-1}v)$.
    
    Combining these, we get:
    $P^{\sigma(A), \gamma}(v) = \sigma_{x_0} P^{A, \gamma}(\sigma_{x_0}^{-1}v) = (\sigma_{x_0} \circ P^{A, \gamma} \circ \sigma_{x_0}^{-1})(v)$.
    
    As this holds for all $v \in E_{x_0}$, the desired identity follows.
4. Recall the local formula for the curvature
  
   $$F_{i j}=\frac{\partial A_j}{\partial x^i}-\frac{\partial A_i}{\partial x^j}+\left[A_i, A_j\right],$$

   where we view $A_i=\left(A_{i \beta}^\alpha\right)$ and $F_{i j}=\left(F_{i j}{ }^\alpha{ }_\beta\right)$ as matrix-valued functions.

   Check directly using the transformation rule for $A_i$ (from class) that $F_{i j}$ transforms under a change of frame as:
   
   $$F_{i j}'=\sigma \cdot F_{i j} \cdot \sigma^{-1}$$


   **Proof**

    Let $A_i' = \sigma A_i \sigma^{-1} - (\partial_i \sigma) \sigma^{-1}$ be the transformed connection 1-form under the gauge transformation $\sigma$. We want to show that the curvature transforms as $F_{ij}' = \sigma F_{ij} \sigma^{-1}$.
    
    We compute $F_{ij}'$ using the definition of curvature:
    
    $`\begin{aligned}
   F'_{ij} &= \partial_i A'_j - \partial_j A'_i + [A'_i, A'_j] \\
   &= \partial_i(\sigma A_j \sigma^{-1} - (\partial_j\sigma)\sigma^{-1}) - \partial_j(\sigma A_i \sigma^{-1} - (\partial_i\sigma)\sigma^{-1}) + [\sigma A_i \sigma^{-1} - (\partial_i\sigma)\sigma^{-1}, \sigma A_j \sigma^{-1} - (\partial_j\sigma)\sigma^{-1}]
   \end{aligned}`$

    Expanding this expression and using the fact that the derivative of the inverse matrix is given by $`D(\sigma^{-1}) = -\sigma^{-1} D(\sigma) \sigma^{-1}`$, we find that the terms involving derivatives of $\sigma$ cancel out, the terms involving only $A$ and its derivatives combine to $\sigma F_{ij} \sigma^{-1}$. All other terms, which involve $\sigma$ and its derivatives, cancel out. For example, $(\partial_i\sigma)A_j\sigma^{-1}$ from $\partial_i A_j'$ cancels with $- (\partial_i\sigma)A_j\sigma^{-1}$ from the commutator term. Similarly, all other mixed terms cancel in pairs.

   $`\begin{aligned}&((\partial_i σ) A_j + σ \partial_i (A_j)) σ^{-1} - (σ A_j σ^{-1} (\partial_i σ) σ^{-1}) -
        (\partial_i (\partial_j σ) σ^{-1} - ((\partial_j σ) σ^{-1} (\partial_i σ) σ^{-1})) \\&-
      (((\partial_j σ) A_i + σ \partial_j (A_i)) σ^{-1} - (σ A_i σ^{-1} (\partial_j σ) σ^{-1}) -
        (\partial_i (\partial_j σ) σ^{-1} - ((\partial_i σ) σ^{-1} (\partial_j σ) σ^{-1}))) \\&+
    (σ A_i A_j σ^{-1} - (\partial_i σ) A_j σ^{-1} -
        (σ A_i σ^{-1} (\partial_j σ) σ^{-1} - (\partial_i σ) σ^{-1} (\partial_j σ) σ^{-1}) \\&-
      (σ A_j A_i σ^{-1} - (\partial_j σ) A_i σ^{-1} -
        (σ A_j σ^{-1} (\partial_i σ) σ^{-1} - (\partial_j σ) σ^{-1} (\partial_i σ) σ^{-1}))) =
  σ (\partial_i (A_j) - \partial_j (A_i) + (A_i A_j - A_j A_i)) σ^{-1}\end{aligned}`$

    $`F_{ij}' = \sigma (\partial_i A_j - \partial_j A_i + [A_i, A_j]) \sigma^{-1} = \sigma F_{ij} \sigma^{-1}`$
    
    Thus, we have shown that the curvature transforms as claimed under a change of frame.

5. Let $E$ be the rank 2 bundle over $S^2$ with transition function
    
    $`\sigma=\frac{1}{\sqrt{x^2+y^2}}\left(\begin{array}{cc}x & -y \\y & x\end{array}\right)`$
    
    on the overlap of the two stereographic charts $`U_0=\{(x, y)\}`$ and $`U_1=\{(\tilde{x}, \tilde{y})\}`$. Consider the following collection of connection matrices:
    
    $`A_1=\frac{1}{1+x^2+y^2}\left(\begin{array}{cc}0 & y \\-y & 0\end{array}\right), \quad A_2=\frac{1}{1+x^2+y^2}\left(\begin{array}{cc}0 & -x \\x & 0\end{array}\right)`$
    
    over $U_0$, and
    
    $`\tilde{A}_1=\frac{1}{1+\tilde{x}^2+\tilde{y}^2}\left(\begin{array}{cc}0 & -\tilde{y} \\\tilde{y} & 0\end{array}\right), \quad \tilde{A}_2=\frac{1}{1+\tilde{x}^2+\tilde{y}^2}\left(\begin{array}{cc}0 & \tilde{x} \\-\tilde{x} & 0\end{array}\right)`$
    
    over $U_1$.

<ol type="a">
<li>
  
  Show that these matrices define a connection $A$ on $E$.

  **Proof**
  
  The stereographic charts are related by $\tilde{x} = \frac{x}{x^2+y^2}$, $\tilde{y} = \frac{y}{x^2+y^2}$, so $x = \frac{\tilde{x}}{\tilde{x}^2+\tilde{y}^2}$, $y = \frac{\tilde{y}}{\tilde{x}^2+\tilde{y}^2}$.

  Let $r^2 = x^2 + y^2$, $\tilde{r}^2 = \tilde{x}^2 + \tilde{y}^2 = 1/r^2$.

  $$\frac{\partial x}{\partial \tilde{x}} = \frac{\tilde{y}^2 - \tilde{x}^2}{\tilde{r}^4}, \quad \frac{\partial x}{\partial \tilde{y}} = \frac{-2\tilde{x}\tilde{y}}{\tilde{r}^4}$$

  $$\frac{\partial y}{\partial \tilde{x}} = -\frac{2\tilde{x}\tilde{y}}{\tilde{r}^4}, \quad \frac{\partial y}{\partial \tilde{y}} = \frac{\tilde{x}^2 - \tilde{y}^2}{\tilde{r}^4}$$

  With $`\sigma = \frac{1}{r}\begin{pmatrix} x & -y \\ y & x \end{pmatrix}=\frac{1}{\tilde{r}}\begin{pmatrix} \tilde{x} & -\tilde{y} \\ \tilde{y} & \tilde{x} \end{pmatrix}$, we have $\sigma^{-1} = \frac{1}{\tilde{r}}\begin{pmatrix} \tilde{x} & \tilde{y} \\ -\tilde{y} & \tilde{x} \end{pmatrix}`$.

  $`\frac{\partial \sigma^{-1}}{\partial \tilde{x}} = -\frac{\tilde{x}}{\tilde{r}^3}\begin{pmatrix}\tilde{x}&\tilde{y}\\-\tilde{y}&\tilde{x}\end{pmatrix} + \frac{1}{\tilde{r}}\begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix} = \frac{\tilde{y}}{\tilde{r}^3}\begin{pmatrix}\tilde{y}&-\tilde{x}\\\tilde{x}&\tilde{y}\end{pmatrix}`$

  $`\frac{\partial \sigma^{-1}}{\partial \tilde{y}}=-\frac{\tilde{y}}{\tilde{r}^3}\begin{pmatrix}\tilde{x}&\tilde{y}\\-\tilde{y}&\tilde{x}\end{pmatrix} + \frac{1}{\tilde{r}}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix} = -\frac{\tilde{x}}{\tilde{r}^3}\begin{pmatrix}\tilde{y}&-\tilde{x}\\\tilde{x}&\tilde{y}\end{pmatrix}`$

  Computing the products:

  $`\sigma \frac{\partial \sigma^{-1}}{\partial \tilde{x}} = -\frac{\tilde{y}}{\tilde{r}^2}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix}, \quad \sigma \frac{\partial \sigma^{-1}}{\partial \tilde{y}} = \frac{\tilde{x}}{\tilde{r}^2}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix}`$

  Rewriting $A_j$ in $\tilde{r}$ coordinates

  $`A_1 = \frac{1}{1+\tilde{r}^2}\begin{pmatrix} 0 & \tilde{y} \\ -\tilde{y} & 0 \end{pmatrix}, \qquad A_2 = \frac{1}{1+\tilde{r}^2}\begin{pmatrix} 0 & -\tilde{x} \\ \tilde{x} & 0 \end{pmatrix}`$

  The connection 1-forms transform as:

  $`\tilde{A}_i = \sigma \frac{\partial \sigma^{-1}}{\partial \tilde{x}^i} + \sum_j\sigma\, A_j\, \sigma^{-1} \frac{\partial x^j}{\partial \tilde{x}^i}`$

  Since $`\sigma = \frac{1}{\tilde{r}}(\tilde{x}I + \tilde{y}J)$ where $J = \begin{pmatrix}0&-1\\1&0\end{pmatrix}`$, and the $A_j$ are proportional to $`-J = \begin{pmatrix}0&1\\-1&0\end{pmatrix}`$, we use $J^2 = -I$ to conclude $\sigma J \sigma^{-1} = J$, hence $`\sigma\, A_j\, \sigma^{-1} = A_j`$. The formula simplifies to:

  $$\tilde{A}_i = \sigma \frac{\partial \sigma^{-1}}{\partial \tilde{x}^i} + \sum_j A_j \frac{\partial x^j}{\partial \tilde{x}^i}$$

  Verification for $i=1$

  $`\sum_j A_j \frac{\partial x^j}{\partial \tilde{x}} = \frac{\tilde{y}}{1+\tilde{r}^2}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix} \frac{\tilde{y}^2 - \tilde{x}^2}{\tilde{r}^4} + \frac{-\tilde{x}}{1+\tilde{r}^2}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix} \frac{-2\tilde{x}\tilde{y}}{\tilde{r}^4} = \frac{\tilde{y}}{\tilde{r}^2(1+\tilde{r}^2)}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix}`$

  Adding the two contributions:

  $`\sigma\frac{\partial \sigma^{-1}}{\partial \tilde{x}} + \sum_j A_j \frac{\partial x^j}{\partial \tilde{x}} = \frac{\tilde{y}}{\tilde{r}^2}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix}\left[-1 + \frac{1}{1+\tilde{r}^2}\right] = \frac{\tilde{y}}{\tilde{r}^2}\cdot\frac{-\tilde{r}^2}{1+\tilde{r}^2}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix} = \frac{1}{1+\tilde{r}^2}\begin{pmatrix} 0 & -\tilde{y} \\ \tilde{y} & 0 \end{pmatrix} = \tilde{A}_1\;\checkmark`$

  Verification for $i=2$

  $`\sum_j A_j \frac{\partial x^j}{\partial \tilde{y}} = \frac{\tilde{y}}{1+\tilde{r}^2}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix} \frac{-2\tilde{x}\tilde{y}}{\tilde{r}^4} + \frac{-\tilde{x}}{1+\tilde{r}^2}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix} \frac{\tilde{x}^2 - \tilde{y}^2}{\tilde{r}^4} = -\frac{\tilde{x}}{\tilde{r}^2(1+\tilde{r}^2)}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix}`$

  Adding the two contributions:

  $`\sigma\frac{\partial \sigma^{-1}}{\partial \tilde{y}} + \sum_j A_j \frac{\partial x^j}{\partial \tilde{y}} = \frac{\tilde{x}}{\tilde{r}^2}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix}\left[1 - \frac{1}{1+\tilde{r}^2}\right] = \frac{\tilde{x}}{\tilde{r}^2}\cdot\frac{\tilde{r}^2}{1+\tilde{r}^2}\begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix} = \frac{1}{1+\tilde{r}^2}\begin{pmatrix} 0 & \tilde{x} \\ -\tilde{x} & 0 \end{pmatrix} = \tilde{A}_2\;\checkmark`$

  Since $`\tilde{A}_i = \sigma\,\partial_{\tilde{x}^i}\sigma^{-1} + \sigma\, A_j\,\sigma^{-1}\,\frac{\partial x^j}{\partial \tilde{x}^i}`$ holds for $i=1,2$, the connection matrices $`\{A_1,A_2\}`$ on $U_0$ and $`\{\tilde{A}_1,\tilde{A}_2\}`$ on $U_1$ satisfy the correct gauge-transformation law and therefore define a global connection $A$ on the bundle $E$.
</li>
<li>

Calculate the curvature components $F_{i j}$ in the chart $U_0$.

**Proof**

We compute the curvature using the formula:
$F_{ij} = \partial_i A_j - \partial_j A_i + [A_i, A_j]$

$`\partial_1 A_2 = \partial_1 \left( \frac{1}{1+x^2+y^2} \begin{pmatrix} 0 & -x \\ x & 0 \end{pmatrix} \right) = \partial_1 \left( \frac{1}{1+x^2+y^2} \right) \begin{pmatrix} 0 & -x \\ x & 0 \end{pmatrix} + \frac{1}{1+x^2+y^2} \partial_1 \begin{pmatrix} 0 & -x \\ x & 0 \end{pmatrix}= -\frac{2x}{(1+x^2+y^2)^2} \begin{pmatrix} 0 & -x \\ x & 0 \end{pmatrix} + \frac{1}{1+x^2+y^2} \begin{pmatrix} 0 & -1 \\ 1 & 0 \end{pmatrix}= \frac{1-x^2+y^2}{(1+x^2+y^2)^2} \begin{pmatrix} 0 & -1 \\ 1 & 0 \end{pmatrix}`$

$`\partial_2 A_1 = \partial_2 \left( \frac{1}{1+x^2+y^2} \begin{pmatrix} 0 & y \\ -y & 0 \end{pmatrix} \right) = \partial_2 \left( \frac{1}{1+x^2+y^2} \right) \begin{pmatrix} 0 & y \\ -y & 0 \end{pmatrix} + \frac{1}{1+x^2+y^2} \partial_2 \begin{pmatrix} 0 & y \\ -y & 0 \end{pmatrix}= -\frac{2y}{(1+x^2+y^2)^2} \begin{pmatrix} 0 & y \\ -y & 0 \end{pmatrix} + \frac{1}{1+x^2+y^2} \begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix}=-\frac{1+x^2-y^2}{(1+x^2+y^2)^2} \begin{pmatrix} 0 & -1 \\ 1 & 0 \end{pmatrix}`$

$`[A_1, A_2] = A_1 A_2 - A_2 A_1= \frac{1}{1+x^2+y^2}\left(\begin{array}{cc}0 & y \\-y & 0\end{array}\right) \frac{1}{1+x^2+y^2}\left(\begin{array}{cc}0 & -x \\x & 0\end{array}\right) - \frac{1}{1+x^2+y^2}\left(\begin{array}{cc}0 & -x \\x & 0\end{array}\right) \frac{1}{1+x^2+y^2}\left(\begin{array}{cc}0 & y \\-y & 0\end{array}\right)=\frac{1}{(1+x^2+y^2)^2}\left(\begin{array}{cc}xy & 0 \\0 & xy\end{array}\right)-\frac{1}{(1+x^2+y^2)^2}\left(\begin{array}{cc}xy & 0 \\0 & xy\end{array}\right)=0`$

$`F_{12} = \partial_1 A_2 - \partial_2 A_1 = \frac2{(1+x^2+y^2)^2} \begin{pmatrix} 0 & -1 \\ 1 & 0 \end{pmatrix}`$
</li>
<li>
  
  Show that there is a global automorphism of $E$ given by $`I=\left(\begin{array}{cc}0 & -1 \\ 1 & 0\end{array}\right)`$ in either chart.

  **Proof**

  We check that $I$ is well-defined on the overlap of the two charts. The transition function $\sigma$ is given by $`\sigma = \frac{1}{\sqrt{x^2+y^2}}\begin{pmatrix} x & -y \\ y & x \end{pmatrix}`$, and we want to verify that $I$ commutes with $\sigma$: Noting that $\sigma = \frac{1}{r}(xI_2 + yJ)$ with $`J = I = \begin{pmatrix}0&-1\\1&0\end{pmatrix}`$ gives the commutation immediately since $I$ commutes with both $I_2$ and itself.

</li>
<li>
  
  Show that $F_A=\frac{1}{2} d V_{\mathbb{S}^2} \otimes I$.

  **Proof**

  With the curvature from part (b), $`F_{12} = \frac{2}{(1+r^2)^2}\begin{pmatrix}0&-1\\1&0\end{pmatrix} = \frac{2}{(1+r^2)^2}I`$, and the standard area form on $S^2$ in stereographic coordinates is $`dV_{\mathbb{S}^2} = \frac{4}{(1+r^2)^2}\,dx\wedge dy`$,
  
  $`F_A = F_{12}\,dx\wedge dy = \frac{2}{(1+r^2)^2}\,I\,dx\wedge dy = \frac{1}{2}\cdot\frac{4}{(1+r^2)^2}\,dx\wedge dy\otimes I = \frac{1}{2}\,dV_{\mathbb{S}^2}\otimes I`$
</li>
</ol>

6. Let $\nabla=\pi_{T S^n} \circ \nabla^{\circ}$ be the connection on $T S^n$ discussed in class. Derive the following formulae, valid for vector fields $X$ and $Y$ tangent to $S^n \subset \mathbb{R}^{n+1}$ :

(a) $\nabla_X Y=X(Y)+\langle X, Y\rangle \vec{r}$.

(b) $X(\vec{r})=X$

(c) $\nabla_X Y-\nabla_Y X=[X, Y]$.

**Proof**

(b) $X(\vec{r}) = X$

The position vector $\vec{r}$ is the map $(x^1,\dots,x^{n+1})$. If $`X = \sum X^i\,\partial/\partial x^i`$, then

$$X(\vec{r}) = \bigl(X(x^1),\dots,X(x^{n+1})\bigr) = (X^1,\dots,X^{n+1}) = X.$$

(a) Differentiate the constraint $\langle Y,\vec{r}\rangle = 0$ along $X$:

$`0 = X\langle Y,\vec{r}\rangle = \langle X(Y),\,\vec{r}\rangle + \langle Y,\,X(\vec{r})\rangle.`$

Using part (b), we have $\langle X(Y),\vec{r}\rangle = -\langle Y,X\rangle$. To compute $\nabla_X Y$, we project $X(Y)$ onto the tangent space of $S^n$ at the point. The normal component is given by $\langle X(Y),\vec{r}\rangle \vec{r}$, so the tangential component is

$\nabla_X Y = X(Y) - \langle X(Y),\vec{r}\rangle \vec{r} = X(Y) + \langle Y,X\rangle \vec{r}.$

(c) Applying the formula from (a) to both terms:

$`\nabla_X Y - \nabla_Y X = \bigl(X(Y) + \langle X,Y\rangle\,\vec{r}\bigr) - \bigl(Y(X) + \langle Y,X\rangle\,\vec{r}\bigr).`$

Since the inner product is symmetric, $\langle X,Y\rangle = \langle Y,X\rangle$, the $\vec{r}$-terms cancel:

$$\nabla_X Y - \nabla_Y X = X(Y) - Y(X) = [X,Y].$$

Since $X$ and $Y$ are tangent to $S^n$, their Lie bracket $[X,Y]$ on $S^n$ coincides with the restriction of $[\tilde{X},\tilde{Y}]$ to $S^n$ for any ambient extensions $\tilde{X},\tilde{Y}$. And the directional derivatives $X(Y), Y(X)$ along $S^n$ depend only on the values of $Y, X$ along curves in $S^n$, so they equal the ambient derivatives $`\nabla^{\circ}_{\tilde{X}}\tilde{Y}|_{S^n}`$ and $`\nabla^{\circ}_{\tilde{Y}}\tilde{X}|_{S^n}`$ respectively.
