# 1.1
Let $E=\mathbb{Q}[\alpha]$, where $\alpha^3-\alpha^2+\alpha+2=0$. Express $\left(\alpha^2+\alpha+1\right)\left(\alpha^2-\alpha\right)$ and $(\alpha-1)^{-1}$ in the form $a \alpha^2+b \alpha+c$ with $a, b, c \in \mathbb{Q}$.

**Proof.** We have $\alpha^3=\alpha^2-\alpha-2$. Thus

$`\begin{aligned}\left(\alpha^2+\alpha+1\right)\left(\alpha^2-\alpha\right) & =\alpha^4-\alpha^3+\alpha^3-\alpha^2+\alpha^2-\alpha \\ & =\alpha^4-\alpha \\ & =\alpha \cdot \alpha^3-\alpha \\ & =\alpha\cdot\left(\alpha^2-\alpha-2\right)-\alpha \\ & =\alpha^3-\alpha^2-3\alpha \\ & =\left(\alpha^2-\alpha-2\right)-\alpha^2-3\alpha \\ & =-4\alpha-2.\end{aligned}`$

For the second part, let $a, b, c \in \mathbb{Q}$ such that $(\alpha-1)^{-1}=a \alpha^2+b \alpha+c$. Then $1=(\alpha-1)(a \alpha^2+b \alpha+c)=a \alpha^3+(b-a) \alpha^2+(c-b) \alpha-c$. Substituting $\alpha^3=\alpha^2-\alpha-2$ gives $1=b \alpha^2+(-a+c-b) \alpha-2 a-c$. Thus $b=0,a=c=-1/3$, and $(\alpha-1)^{-1}=-\frac13-\frac{1}{3}\alpha^2$.

# 1.2
Determine $[\mathbb{Q}(\sqrt{2}, \sqrt{3}): \mathbb{Q}]$.

**Proof.** We have $\mathbb{Q}(\sqrt{2}, \sqrt{3})=\mathbb{Q}(\sqrt{2})(\sqrt{3})$. Since $[\mathbb{Q}(\sqrt{2}): \mathbb{Q}]=2$ and $\sqrt{3}$ is not in $\mathbb{Q}(\sqrt{2})$ [Suppose $\sqrt{3} = a+b\sqrt{2}$ for $a,b \in \mathbb{Q}$. Squaring gives $3 = a^2+2b^2+2ab\sqrt{2}$. If $ab \neq 0$, then $\sqrt{2} = (3-a^2-2b^2)/(2ab) \in \mathbb{Q}$, a contradiction. If $ab=0$, then either $a^2=3$ or $2b^2=3$, both impossible for $a,b \in \mathbb{Q}$], we have $[\mathbb{Q}(\sqrt{2})(\sqrt{3}): \mathbb{Q}(\sqrt{2})]=2$. By the tower law, $[\mathbb{Q}(\sqrt{2}, \sqrt{3}): \mathbb{Q}]=[\mathbb{Q}(\sqrt{2})(\sqrt{3}): \mathbb{Q}(\sqrt{2})][\mathbb{Q}(\sqrt{2}): \mathbb{Q}]=4$.

# 1.5
Let $f(X)$ be an irreducible polynomial over $F$ of degree $n$, and let $E$ be a field extension of $F$ with $[E: F]=m$. If $\gcd(m, n)=1$, show that $f$ is irreducible over $E$.

Let $\alpha$ be a root of $f$ in some extension of $E$. Since $f$ is irreducible over $F$ and $\deg(f) = n$, the degree of the extension is $[F(\alpha): F] = n$. Let $m = [E: F]$. By the tower law, we consider the extension $E(\alpha)$ over $F$:
$[E(\alpha): F] = [E(\alpha): E][E: F] = [E(\alpha): E] \cdot m$
$[E(\alpha): F] = [E(\alpha): F(\alpha)][F(\alpha): F] = [E(\alpha): F(\alpha)] \cdot n$
Equating these, we have $[E(\alpha): E] \cdot m = [E(\alpha): F(\alpha)] \cdot n$. Since $\gcd(m, n) = 1$, $n$ must divide $[E(\alpha): E] \cdot m$. By Euclid's Lemma, $n$ must divide $[E(\alpha): E]$. Since $\alpha$ is a root of $f \in E[X]$, the degree of the minimal polynomial of $\alpha$ over $E$ satisfies $[E(\alpha): E] \leq \deg(f) = n$. Given $n$ divides $[E(\alpha): E]$ and $1 \leq [E(\alpha): E] \leq n$, it follows that $[E(\alpha): E] = n$. This implies the minimal polynomial of $\alpha$ over $E$ has degree $n$. Since $f$ is a monic polynomial of degree $n$ in $E[X]$ and $f(\alpha) = 0$, $f$ is the minimal polynomial and is thus irreducible over $E$.

---
The following two problems are about the composite of fields, defined in the textbook.

# Problem A
Let $F, E_1, E_2$ be subfields of $K$. Suppose $E_1 \supset F$ and $E_2 \supset F$. Show that if $E_1 / F$ is a finite extension, then so is $E_1 E_2 / E_2$, and moreover, $[E_1 E_2: E_2] \leq \left[ E_1: F \right]$. 

Proof. Let $[E_1: F]=n$ and let $\alpha_1, \ldots, \alpha_n$ be a basis of $E_1$ over $F$. The composite field $E_1 E_2$ is the smallest field containing $E_1$ and $E_2$, which is the field of fractions of the ring $E_2[E_1]$. Since $E_1 = \sum_{i=1}^n F \alpha_i$, any element in $E_2[E_1]$ is an $E_2$-linear combination $\sum_{i=1}^n c_i \alpha_i$ with $c_i \in E_2$. Thus $E_2[E_1]$ is a finite-dimensional $E_2$-vector space. Since it is an integral domain, it is a field, so $E_1 E_2 = E_2[E_1]$. This shows that $\alpha_1, \ldots, \alpha_n$ span $E_1 E_2$ over $E_2$, so $[E_1 E_2: E_2] \leq n=[E_1: F]$.

# Problem B
In the situation of the previous problem, show that if $E_1 / F$ is algebraic, then so is $E_1 E_2 / E_2$.
