# 1
The Clifford torus $\Sigma$ is the embedded surface in $\mathbb{R}^3$ parametrized by

$$
\sigma(u, v)=((\sqrt{2}+\cos (v)) \cos (u),(\sqrt{2}+\cos (v)) \sin (u), \sin (v)) .
$$

(a) Compute the Gauss curvature of $\Sigma$.

(b) Compute the integral of the Gauss curvature with respect to the induced volume form.

**Proof**

(a) $\sigma_u = (-(\sqrt{2}+\cos(v)) \sin(u), (\sqrt{2}+\cos(v)) \cos(u), 0)$ and $\sigma_v = (-\sin(v) \cos(u), -\sin(v) \sin(u), \cos(v))$.

$E = \langle \sigma_u, \sigma_u \rangle = (\sqrt{2}+\cos(v))^2$,

$F = \langle \sigma_u, \sigma_v \rangle = 0$, and $G = \langle \sigma_v, \sigma_v \rangle = 1$

$\mathbf{n} = \frac{\sigma_u \times \sigma_v}{\|\sigma_u \times \sigma_v\|} = (\cos u \cos v, \sin u \cos v, \sin v)$

$\sigma_{uu} = (-(\sqrt{2}+\cos v) \cos u, -(\sqrt{2}+\cos v) \sin u, 0)$,
$\sigma_{uv} = (\sin v \sin u, -\sin v \cos u, 0)$,
and $\sigma_{vv} = (-\cos v \cos u, -\cos v \sin u, -\sin v)$.

$L = \langle \sigma_{uu}, \mathbf{n} \rangle = -(\sqrt{2}+\cos v)\cos v$, $M = \langle \sigma_{uv}, \mathbf{n} \rangle = 0$, and $N = \langle \sigma_{vv}, \mathbf{n} \rangle = -1$

The Gauss curvature is then computed as:

$$
K = \frac{LN - M^2}{EG - F^2} = \frac{[-(\sqrt{2}+\cos v)\cos v][-1] - 0^2}{(\sqrt{2}+\cos v)^2 \cdot 1 - 0^2} = \frac{\cos v}{\sqrt{2}+\cos v}
$$

(b) The integral of the Gauss curvature with respect to the induced volume form is given by:

$$
\int_{\Sigma} K \ dA = \int_0^{2\pi} \int_0^{2\pi} \frac{\cos v}{\sqrt{2}+\cos v} \sqrt{EG - F^2} \ du \ dv = \int_0^{2\pi} \int_0^{2\pi} \cos v \ du \ dv = \left( \int_0^{2\pi} du \right) \left( \int_0^{2\pi} \cos v \ dv \right) = 2\pi \cdot 0 = 0
$$

# 2
Compute the Gauss and mean curvatures of a surface of revolution of $z=f(r)>0$ around the $z$-axis in $\mathbb{R}^3$.

**Proof**

Let the surface of revolution be parametrized by:

$$\sigma(r, v) = (r \cos v, r \sin v, f(r)),$$

where $r$ is the radial coordinate and $v$ is the angular coordinate. We compute the first and second fundamental forms to find the Gauss and mean curvatures.

$\sigma_r = (\cos v, \sin v, f'(r))$ and $\sigma_v = (-r \sin v, r \cos v, 0)$.

$E = \langle \sigma_r, \sigma_r \rangle = 1 + f'(r)^2$, $F = \langle \sigma_r, \sigma_v \rangle = 0$, and $G = \langle \sigma_v, \sigma_v \rangle = r^2$.

The normal vector is given by:

$$\mathbf{n} = \frac{\sigma_r \times \sigma_v}{\|\sigma_r \times \sigma_v\|} = \frac{(-f'(r) \cos v, -f'(r) \sin v, 1)}{\sqrt{1 + f'(r)^2}}.$$

$\sigma_{rr} = (0, 0, f''(r))$, $\sigma_{rv} = (-\sin v, \cos v, 0)$, and $\sigma_{vv} = (-r \cos v, -r \sin v, 0)$.

$L = \langle \sigma_{rr}, \mathbf{n} \rangle = \frac{f''(r)}{\sqrt{1 + f'(r)^2}}$, $M = \langle \sigma_{rv}, \mathbf{n} \rangle = 0$, and $N = \langle \sigma_{vv}, \mathbf{n} \rangle = \frac{r f'(r)}{\sqrt{1 + f'(r)^2}}$.

The Gauss curvature is computed as:

$$K = \frac{LN - M^2}{EG - F^2} = \frac{\frac{r f'(r) f''(r)}{1 + f'(r)^2}}{r^2 (1 + f'(r)^2)} = \frac{f'(r) f''(r)}{r (1 + f'(r)^2)^2}.$$

The mean curvature is computed as:

$$H = \frac{1}{2} \frac{EN - 2FM + GL}{EG - F^2} = \frac{1}{2} \frac{(1 + f'(r)^2) \frac{r f'(r)}{\sqrt{1 + f'(r)^2}} + r^2 \frac{f''(r)}{\sqrt{1 + f'(r)^2}}}{r^2 (1 + f'(r)^2)} = \frac{f'(r)(1 + f'(r)^2) + r f''(r)}{2r (1 + f'(r)^2)^{3/2}}.$$

The case $r=0$ should be analyzed separately to ensure the surface is regular:

Since $\mathbf{n}$ should be independent of $v$ at a point on the rotation axis, we must have $f'(0)=0$.

To ensure $K$ and $H$ are well-defined in the limit, we use $\lim_{r \to 0} \frac{f'(r)}{r} = f''(0)$.

# 5
Suppose that a surface $S \subset \mathbb{R}^3$ contains a straight line $L$. Show that the Gauss curvature is nonpositive at all points along $L$.

**Proof**

Let $p \in L$ and let $\mathbf{v}$ be a unit vector along $L$. Parametrize the line by arc length,
$$
\gamma(s) = p + s\mathbf{v}, \qquad \gamma(0) = p.
$$
Since $L \subset S$, the curve $\gamma$ lies entirely in $S$, with
$$
\gamma'(0) = \mathbf{v} \in T_p S, \qquad \gamma''(s) \equiv 0.
$$
Let $N$ be a smooth unit normal to $S$ near $p$. The normal curvature of $S$ at $p$ in the direction $\mathbf{v}$ is
$$
k_n(\mathbf{v}) = \mathrm{II}_p(\mathbf{v}, \mathbf{v}) = \langle \gamma''(0), N(p)\rangle = 0.
$$
The principal curvatures are extrema of normal curvatures at $p$. Since $0$ is attained as a normal curvature, we conclude
$$
k_1(p) \leq 0 \leq k_2(p).
$$
Therefore
$$
K(p) = k_1(p)\, k_2(p) \leq 0,
$$
as desired.
