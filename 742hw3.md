# 3.34
Let $e, e' \in \mathrm{Idem}(R)$. Assume $\sqrt{\langle e\rangle}=\sqrt{\left\langle e'\right\rangle}$. Show $e=e'$.
# 3.35
Let $R$ be a ring, $\mathfrak{a}_1, \mathfrak{a}_2$ comaximal ideals with $\mathfrak{a}_1 \mathfrak{a}_2 \subset \mathrm{nil}(R)$. Show there are complementary idempotents $e_1$ and $e_2$ with $e_i \in \mathfrak{a}_i$.
# 3.36
Let $R$ be a ring, $\mathfrak{a}$ an ideal, $\kappa: R \rightarrow R / \mathfrak{a}$ the quotient map. Assume $\mathfrak{a} \subset \mathrm{nil}(R)$. Show $\mathrm{Idem}(\kappa)$ is bijective.
# 3.37
Let $R$ be a ring. Prove the following statements equivalent:
(1) $R$ has exactly one prime $\mathfrak{p}$;
(2) every element of $R$ is either nilpotent or a unit;
(3) $R / \mathrm{nil}(R)$ is a field.
# 3.42
Let $R$ be a ring, $X$ a variable, $F:=a_0+a_1 X+\cdots+a_n X^n$.
(1) Prove $F$ is nilpotent if and only if $a_0, \ldots, a_n$ are nilpotent.
(2) Prove $F$ is a unit if and only if $a_0$ is a unit and $a_1, \ldots, a_n$ are nilpotent.

# Problem A
An ideal $I$ is said to be nilpotent if $I^n=0$ for some $n$. Show that if $I \subset R$ is nilpotent, then $I \subset \mathrm{nil}(R)$.

Conversely, show that if $I \subset \mathrm{nil}(R)$ is a finitely generated ideal (that is, $I=\left(a_1, \ldots, a_k\right)$ for some $k$ and some $a_i$ ), then $I$ is nilpotent.

# Problem B
Continuing the previous problem, give an example of a ring $R$ and an ideal $I \subset \mathrm{nil}(R)$ that is not a nilpotent ideal.
