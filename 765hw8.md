# 1.
Use your answer from HW 6 #2 to calculate the Riemann tensor for the Poincaré ball model of hyperbolic space.

(Note: your final answer has to be $R_{i j k \ell}=-g_{i k} g_{j \ell}+g_{i \ell} g_{j k}$.)

**Proof**

From HW 6 #2 the Christoffel symbols are:

$\Gamma^k_{ij}= \frac{2}{1 - |x|^2} (x_i \delta_{jk} + x_j \delta_{ik} - x_k \delta_{ij})$

Riemann curvature components

$`
R_{ij}{}^{k}{}_{l} := \partial_i\Gamma^{k}_{jl} - \partial_j\Gamma^{k}_{il} + \Gamma^{k}_{im}\Gamma^{m}_{jl} - \Gamma^{k}_{jm}\Gamma^{m}_{il}.
`$

First observe that if $`|\{i,j,k,l\}| = 4`$ then no terms will survive in Riemann curvature components.

If $`|\{i,j,k,l\}| = 3`$, by symmetry of $R$, we only need to compute $R_{ijil}$ for $i,j,l$ distinct.

$`
    \begin{split}
        R_{ij}{}^i{}_l & = \partial_i \Gamma^i_{jl} - \partial_j \Gamma^i_{il} + \sum_m \Gamma^i_{im} \Gamma^m_{jl} - \Gamma^i_{jm} \Gamma^m_{il} \\
        & = -\partial_j \left( \frac{2 x^l}{1 - |x|^2} \right) + \left( \Gamma^i_{ii} \Gamma^i_{jl} - \Gamma^i_{ji} \Gamma^i_{il} \right) + \left( \Gamma^i_{ij} \Gamma^j_{jl} - \Gamma^i_{jj} \Gamma^j_{il} \right) + \left( \Gamma^i_{il} \Gamma^l_{jl} - \Gamma^i_{jl} \Gamma^l_{il} \right)\\
        & = -\frac{4x^lx^j}{(1-|x|^2)^2} - \Gamma^i_{ji}\Gamma^i_{il} + \Gamma^i_{ij}\Gamma^j_{jl} + \Gamma^i_{il}\Gamma^l_{jl} \\
        & = -\frac{4x^lx^j}{(1-|x|^2)^2} - \frac{4x^j x^l}{(1-|x|^2)^2} + \frac{4x^j x^l}{(1-|x|^2)^2} + \frac{4x^j x^l}{(1-|x|^2)^2}\\
        & = 0
    \end{split}
`$

so $`R_{ijil} = g_{ii} R_{ij}{}^i{}_l = 0`$. This is consistent with the expected formula, as for distinct $i,j,l$, we have $-g_{ii} g_{jl} + g_{il} g_{ji} = -g_{ii} 0 + 0\ 0 = 0$.

If $`|\{i,j,k,l\}|=2`$, by symmetry, we only need to compute $R_{ijij}$ for $i,j$ distinct.

$`
    \begin{split}
        R_{ij}{}^i{}_j & = \partial_i \Gamma^i_{jj} - \partial_j \Gamma^i_{ij} + \sum_m (\Gamma^i_{im} \Gamma^m_{jj} - \Gamma^i_{jm} \Gamma^m_{ij}) \\
& = \frac{-2(1-|x|^2)-4(x^i)^2}{(1-|x|^2)^2} - \frac{2(1-|x|^2)+4(x^j)^2}{(1-|x|^2)^2} + \frac{-4\sum_{k \neq i,j}(x^k)^2}{(1-|x|^2)^2}\\
        & = \frac{-4+4|x|^2-4((x^i)^2+(x^j)^2+\sum_{k \neq i,j}(x^k)^2)}{(1-|x|^2)^2} \\
        & = \frac{-4+4|x|^2-4|x|^2}{(1-|x|^2)^2} = \frac{-4}{(1-|x|^2)^2}
    \end{split}
`$

so $`R_{ijij} = g_{ii} R_{ij}{}^i{}_j = \frac{4}{(1-|x|^2)^2} \left( \frac{-4}{(1-|x|^2)^2} \right) = -\frac{16}{(1-|x|^2)^4}`$. This is consistent with the expected formula, as for distinct $i,j$, we have $-g_{ii} g_{jj} + g_{ij} g_{ji} = -g_{ii}g_{jj} = -\left(\frac{4}{(1-|x|^2)^2}\right)^2 = -\frac{16}{(1-|x|^2)^4}$.
# 2.
Show that the Ricci and Scalar curvatures of the sphere $g=g_{S^n}$ are given respectively by

$R c=(n-1) g, \quad S=n(n-1) .$

**Proof**

The Riemann curvature tensor is given by $R_{ijkl} = g_{ik}g_{jl} - g_{il}g_{jk}$ for the unit sphere, and raising an index, we get:

$`R_{ij}{}^k{}_l =g^{km}R_{ijml}= \delta_i^k g_{jl} - \delta_j^k g_{il}
`$

The Ricci tensor is found by contracting on $k$ and $i$:

$`
\begin{aligned}
Rc_{jl} &= \sum_{k=1}^n R_{kj}{}^k{}_l \\
&= \sum_{k=1}^n (\delta_k^k g_{jl} - \delta_j^k g_{kl}) \\
&= n g_{jl} - g_{jl} \\
&= (n-1)g_{jl}
\end{aligned}
`$

Thus, $Rc = (n-1)g$.

The scalar curvature is found by contracting the Ricci tensor:

$`
\begin{aligned}
S &= g^{jl}Rc_{jl} \\
&= g^{jl}(n-1)g_{jl} \\
&= (n-1) \sum_{j,l} g^{jl}g_{jl} \\
&= (n-1) \sum_j \delta_j^j \\
&= n(n-1)
\end{aligned}
`$
# 3.
Show that the sectional curvature at any point of a Riemannian 2-manifold is equal to $S / 2$.

**Proof**

In an orthonormal basis, $g_{ij} = \delta_{ij}$ and $g^{ij} = \delta^{ij}$. The Ricci tensor is $R_{ij} = \sum_k R_{ki}{}^k{}_j$.

For a 2-manifold, the scalar curvature is:

$$
S = R_{11} + R_{22} = \sum_k R_{k1}^k{}_1 + \sum_k R^k_{2k2} = R^2_{121} + R^1_{212}
$$

Using $R^k_{ijl} = g^{km}R_{mijl}$ and symmetries of the Riemann tensor:

$$
S = g^{2m}R_{m121} + g^{1m}R_{m212} = R_{2121} + R_{1212} = 2 R_{1212}
$$

The sectional curvature is $K = K(e_1, e_2) = R(e_1, e_2, e_1, e_2) = R_{1212}$.

Therefore, $S = 2K$, or $K = S/2$.
