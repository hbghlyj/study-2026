The following problems are based on the material from week 2.
1. Let $B \in \Gamma(\mathrm{End} E)$ be a section, with local components $B^\alpha{ }_\beta$.
    
    Suppose that $\sigma=\left(\sigma^\alpha{ }_\beta\right)$ is a change-of-frame. Show that, in matrix notation, the components of $B$ in the new frame are given by
    
    $B'=\sigma \cdot B \cdot \sigma^{-1} .$
    
    **Proof**
    
    Let a vector have components $x$ in the original frame and $x'$ in the new frame. The change-of-frame matrix $\sigma$ transforms the components from the old frame to the new one:
    
    $$x' = \sigma x$$
    
    This implies that to transform from the new frame back to the old one, we use the inverse matrix $\sigma^{-1}$:
    
    $$x = \sigma^{-1} x'$$

    Now, consider the action of the operator $B$ in the original frame, which is described by the equation:
    
    $$y = Bx$$
   
    We want to find the operator $B'$ in the new frame such that:

    $$y' = B'x'$$
        
    To find the relationship between $B$ and $B'$, we can derive it by substituting the known relations:
    
    $$y' = \sigma y = \sigma (Bx) = \sigma B (\sigma^{-1} x') = (\sigma B \sigma^{-1}) x'$$
    
    By comparing this result with the definition $y' = B'x'$, we can identify the components of the operator in the new frame as:
    
    $$B' = \sigma B \sigma^{-1}$$
3. <ol type="a">
    <li>
        
    Let $a \in \Omega^1(\mathrm{End}(E)) = \Gamma(T^*M \otimes \mathrm{End}(E))$ be any smooth section of the bundle of endomorphism-valued 1-forms. If $\nabla$ is a connection on the vector bundle $E \to M$, then the operator
    
    $\nabla' := \nabla + a$
    
    defined pointwise by
    
    $\nabla'_X s = \nabla_X s + a(X) \cdot s$
    
    for all vector fields $X \in \mathfrak{X}(M)$ and sections $s \in \Gamma(E)$, is again a connection on $E$.

    **Proof**
    
    The map $\nabla'$ is $\mathbb{R}$-linear in both arguments and satisfies the Leibniz rule:
    
    $`\begin{align*} \nabla'_X (f s) &= \nabla_X (f s) + a(X) \cdot (f s) \\ &= f \nabla_X s + (X f) s + f \, a(X) \cdot s \\ &= f (\nabla_X s + a(X) \cdot s) + (X f) s \\ &= f \nabla'_X s + (X f) s. \end{align*}`$
    
    Since $a$ is $C^\infty(M)$-linear in $X$,

    $`\begin{align*} \nabla'_{fX} (s)&=\nabla_{fX} (s) + a(fX) \cdot s \\ &= f\nabla_X(s) + f a(X) \cdot s \\ &=f(\nabla_X (s) + a(X) \cdot s)\\[1ex]&=f\nabla'_X(s) \end{align*}`$
    
    </li><li>
    
    If $\nabla$ and $\nabla'$ are any two connections on the same bundle, then their difference is an element $\phi \in \Gamma(T^*M \otimes \mathrm{End}(E))$, which is tensorial (i.e., an $\mathrm{End}(E)$-valued 1-form on $M$).
    
    **Proof**
    
    By the tensor characterization lemma, it suffices to check: For fixed $X \in \Gamma(TM)$, the operator $(\nabla_X - \nabla'_X) : \Gamma(E) \to \Gamma(E)$ is $C^\infty(M)$-linear.
    
    $`\begin{aligned}
    (\nabla_X - \nabla'_X)(f s) &= \nabla_X(f s) - \nabla'_X(f s) \\
    &= (X(f)s + f \nabla_X s) - (X(f)s + f \nabla'_X s) \\
    &= f(\nabla_X s - \nabla'_X s) \\
    &= f(\nabla_X - \nabla'_X)s
    \end{aligned}`$
    
    for $f \in C^\infty(M)$ and $s \in \Gamma(E)$.
    </li>
    <li>
        
    For any two connections $\nabla, \nabla' \in \mathcal{A}(E)$ and any smooth function $\lambda \in C^\infty(M,[0,1])$, the operator
    
    $\nabla_\lambda := \lambda \nabla + (1-\lambda) \nabla'$
    
    is a connection on $E$.
    
    **Proof**

    Let $s \in \Gamma(E)$, $X \in \Gamma(TM)$, and $f \in C^\infty(M)$.

    1.  **$\mathbb{R}$-linearity in $s$** is immediate from the linearity of $\nabla$ and $\nabla'$.
    2.  **$C^\infty(M)$-linearity in $X$**: Since $\nabla$ and $\nabla'$ are connections, they are $C^\infty(M)$-linear in $X$. It follows that:
        
        $`(\nabla_\lambda)_{fX} s = \lambda \nabla_{fX} s + (1-\lambda) \nabla'_{fX} s = \lambda f \nabla_X s + (1-\lambda) f \nabla'_X s = f (\nabla_\lambda)_X s`$.

    3.  **Leibniz Rule**: Using the Leibniz rule for $\nabla$ and $\nabla'$:
        
        $(\nabla_\lambda)_X (f s) = \lambda \nabla_X (f s) + (1-\lambda) \nabla'_X (f s)$
        
        $= \lambda ((Xf)s + f \nabla_X s) + (1-\lambda) ((Xf)s + f \nabla'_X s)$
        
        $= (\lambda + 1 - \lambda)(Xf)s + f(\lambda \nabla_X s + (1-\lambda) \nabla'_X s)$
        
        $= (Xf)s + f (\nabla_\lambda)_X s$.

    Thus, $\nabla_\lambda$ is a connection.
    </li>
    </ol>
4. Consider the connection $\nabla$ defined on the product bundle $\underline{\mathbb{R}}\rightarrow \mathbb{R}^1$ with connection matrix

    $A_1(x)=\frac{1}{1+x^2} .$
    
    Write down a parallel (i.e. covariantly constant) global section.

    **Proof**

    A parallel global section $s = s^1(x) e_1$ must satisfy $\nabla_{\frac{\partial}{\partial x^1}} (s^1 e_1) = 0$.
    
    Applying the Leibniz rule and using the given connection where $\nabla_{\frac{\partial}{\partial x^1}} e_1 = \frac{1}{1+x^2}e_1$, we get the differential equation:
    
    $\frac{ds^1}{dx^1} + \frac{s^1}{1+(x^1)^2} = 0$
    
    Solving this separable ODE gives $s^1(x^1) = C e^{-\arctan(x^1)}$.
    
    Thus, a parallel global section (with constant $C=1$) is $s(x) = e^{-\arctan(x^1)} e_1$.
5. Write down an obvious connection on the Möbius bundle. (Note: you should not need to use a partition of unity.)

    **Proof**

   Consider the Möbius bundle over $S^1$ with total space $E=\mathbb{R}^2/\sim$, where the equivalence relation is given by $(t,v)\sim (t',v')$ iff $\exists k\in \mathbb{Z} : t'=t+k,v'=(-1)^kv$.
   
   The projection $\pi:E\to S^1$ is given by $\pi([t,v])=e^{2\pi i t}$.

   To describe the connection in a local trivialization, we can take inspiration from the definition of $s$ to define the trivialization $$\Phi : E\vert_U \stackrel{\cong}{\to} U \times \Bbb R, \qquad [t, v] \mapsto (e^{2\pi i t}, v)$$ on $`S^1 - \{ 1 \}`$. With respect to this trivialization the function $U \to \Bbb R$ corresponding to our section is just $z \mapsto 1$.

