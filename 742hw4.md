Unless stated otherwise, all rings are assumed to be commutative.
# 4.22
Show that a finitely generated free module $F$ has finite rank.

**Proof**

Let $F$ be a finitely generated free module, and let $`\{f_1, \ldots, f_n\}`$ be a finite generating set for $F$. Since $F$ is free, there exists a basis $`\{e_i\}_{i \in I}`$ for $F$. Each generator $f_j$ can be expressed as a linear combination of the basis elements:

$f_j = \sum_{i \in I} r_{ij} e_i,$

where $r_{ij} \in R$ and only finitely many $r_{ij}$ are nonzero for each $j$. Since there are only finitely many $j$, the set $`\{e_i \mid r_{ij} \neq 0 \text{ for some } j\}`$ is finite. Let $`I' = \{i \in I \mid r_{ij} \neq 0 \text{ for some } j\}`$. Then $`\{e_i\}_{i \in I'}`$ is a finite subset of the basis that generates $F$. Since the basis $I$ is minimal and $I'\subseteq I$, it follows that $I' = I$ is finite, and thus $F$ has finite rank equal to the cardinality of $I'$.

# 4.26
Let $L, M$, and $N$ be modules. Consider a diagram

$L \xtofrom[\rho]{\alpha} M \xtofrom[\sigma]{\beta}N$

where $\alpha, \beta, \rho$, and $\sigma$ are homomorphisms. Prove that

$M=L \oplus N \quad \text { and } \quad \alpha=\iota_L, \beta=\pi_N, \sigma=\iota_N, \rho=\pi_L$

if and only if the following relations hold:

$\beta \alpha=0, \beta \sigma=1, \rho \sigma=0, \rho \alpha = 1, \text { and } \alpha \rho+\sigma \beta=1 .$

**Proof**

- Direction from Direct Sum to Relations

    Assume that $M = L \oplus N$ and that $\alpha(l) = (l, 0)$ for $l \in L$, $\beta(l, n) = n$ for $(l, n) \in L \oplus N$, $\sigma(n) = (0, n)$ for $n \in N$, and $\rho(l, n) = l$ for $(l, n) \in L \oplus N$. We will verify the relations:

    1. $\beta \alpha(l) = \beta(l, 0) = 0$ for all $l \in L$, so $\beta \alpha = 0$.
    2. $\beta \sigma(n) = \beta(0, n) = n$ for all $n \in N$, so $\beta \sigma = 1$.
    3. $\rho \sigma(n) = \rho(0, n) = 0$ for all $n \in N$, so $\rho \sigma = 0$.
    4. $\rho \alpha(l) = \rho(l, 0) = l$ for all $l \in L$, so $\rho \alpha = 1$.
    5. $\alpha \rho(l, n) + \sigma \beta(l, n) = \alpha(l) + \sigma(n) = (l, 0) + (0, n) = (l, n)$ for all $(l, n) \in L \oplus N$, so $\alpha \rho + \sigma \beta = 1$.
- Direction from Relations to Direct Sum

    Define the isomorphism $\phi: L \oplus N \rightarrow M$ by $\phi(l, n) = \alpha(l) + \sigma(n)$ for $l \in L$ and $n \in N$. We will show that $\phi$ is an isomorphism.

    - **Injectivity**: Suppose $\phi(l, n) = 0$. Then $\alpha(l) + \sigma(n) = 0$. Applying $\beta$ to both sides gives $\beta \alpha(l) + \beta \sigma(n) = 0$, which simplifies to $0 + n = 0$, so $n = 0$. Applying $\rho$ to both sides gives $\rho \alpha(l) + \rho \sigma(n) = 0$, which simplifies to $l + 0 = 0$, so $l = 0$. Thus, $(l, n) = (0, 0)$, and $\phi$ is injective.
    - **Surjectivity**: Take any $m \in M$. We can write $m = \alpha(\rho(m)) + \sigma(\beta(m))$ using the relation $\alpha \rho + \sigma \beta = 1$. Thus, $m$ is in the image of $\phi$, and $\phi$ is surjective.

    Therefore, $\phi$ is an isomorphism.

# 4.27
Let $L$ be a module, $\Lambda$ a nonempty set, $M_\lambda$ a module for $\lambda \in \Lambda$. Prove that the injections $\iota_\kappa: M_\kappa \rightarrow \bigoplus M_\lambda$ induce an injection

$\bigoplus \mathrm{Hom}\left(L, M_\lambda\right) \hookrightarrow \mathrm{Hom}\left(L, \bigoplus M_\lambda\right),$

and that it is an isomorphism if $L$ is finitely generated.

**Proof**

Define the map $\Phi: \bigoplus \mathrm{Hom}(L, M_\lambda) \rightarrow \mathrm{Hom}(L, \bigoplus M_\lambda)$ by $`\Phi((f_\lambda)_{\lambda \in \Lambda})(l) = (f_\lambda(l))_{\lambda \in \Lambda}`$ for $l \in L$. We will show that $\Phi$ is an injection and that it is surjective if $L$ is finitely generated.

- **Injectivity**: Composing $\Phi$ with the projection maps $\pi_\kappa: \bigoplus M_\lambda \rightarrow M_\kappa$ gives $`\pi_\kappa \circ \Phi((f_\lambda)_{\lambda \in \Lambda}) = f_\kappa`$ for each $\kappa \in \Lambda$. If $`\Phi((f_\lambda)_{\lambda \in \Lambda}) = 0`$, then $f_\kappa = 0$ for all $\kappa$, so $`(f_\lambda)_{\lambda \in \Lambda} = 0`$. Thus, $\Phi$ is injective.
- **Surjectivity**: Let $g \in \mathrm{Hom}(L, \bigoplus M_\lambda)$. Since $L$ is finitely generated, there exists a finite generating set $`\{l_1, \ldots, l_n\}`$ for $L$. For each $\lambda \in \Lambda$, define $f_\lambda: L \rightarrow M_\lambda$ by $f_\lambda(l_i) = \pi_\lambda(g(l_i))$ for $i = 1, \ldots, n$, and extend to all of $L$ by linearity. We must show that $`(f_\lambda)_{\lambda \in \Lambda} \in \bigoplus \mathrm{Hom}(L, M_\lambda)`$. For each generator $l_i$, $g(l_i) \in \bigoplus M_\lambda$, so the set $`\Lambda_i = \{\lambda \in \Lambda \mid \pi_\lambda(g(l_i)) \neq 0\}`$ is finite. Let $\Lambda' = \bigcup_{i=1}^n \Lambda_i$; this is a finite set. For $\lambda \notin \Lambda'$, we have $`f_\lambda(l_i) = \pi_\lambda(g(l_i)) = 0`$ for all $i$. Since the $l_i$ generate $L$, this implies $f_\lambda$ is the zero homomorphism. Thus, only finitely many $f_\lambda$ are non-zero, so $`(f_\lambda)_{\lambda \in \Lambda} \in \bigoplus \mathrm{Hom}(L, M_\lambda)`$. Then $`\Phi((f_\lambda)_{\lambda \in \Lambda})(l_i) = (f_\lambda(l_i))_{\lambda \in \Lambda} = (\pi_\lambda(g(l_i)))_{\lambda \in \Lambda} = g(l_i)`$ for each generator $l_i$. Since the generators span $L$, it follows that $`\Phi((f_\lambda)_{\lambda \in \Lambda}) = g`$. Thus, $\Phi$ is surjective.

# 4.28
Let $\mathfrak{a}$ be an ideal, $\Lambda$ a nonempty set, $M_\lambda$ a module for $\lambda \in \Lambda$. Prove $\mathfrak{a}\left(\bigoplus M_\lambda\right)=\bigoplus \mathfrak{a} M_\lambda$.
Prove $\mathfrak{a}\left(\prod M_\lambda\right)=\prod \mathfrak{a} M_\lambda$ if $\mathfrak{a}$ is finitely generated.

**Proof**

$\mathfrak{a}\left(\bigoplus M_\lambda\right) \subseteq \bigoplus \mathfrak{a} M_\lambda$ : for any $a \in \mathfrak{a}$ and $`m = (m_\lambda)_{\lambda \in \Lambda} \in \bigoplus M_\lambda`$, we have $`a m = (a m_\lambda)_{\lambda \in \Lambda}`$, and each $`a m_\lambda$ is in $\mathfrak{a} M_\lambda`$.

$\bigoplus \mathfrak{a} M_\lambda \subseteq \mathfrak{a}\left(\bigoplus M_\lambda\right)$ : An element $x \in \bigoplus \mathfrak{a} M_\lambda$ is a finite sum of elements of the form $\iota_\lambda(y)$ where $y \in \mathfrak{a}M_\lambda$. Since $y$ is a finite sum $\sum_i a_i m_i$, we have $\iota_\lambda(y) = \sum_i a_i \iota_\lambda(m_i) \in \mathfrak{a}(\bigoplus M_\lambda)$. Thus $x \in \mathfrak{a}(\bigoplus M_\lambda)$.

$\mathfrak{a}\left(\prod M_\lambda\right) \subseteq \prod \mathfrak{a} M_\lambda$ : for any $a \in \mathfrak{a}$ and $`m = (m_\lambda)_{\lambda \in \Lambda} \in \prod M_\lambda`$, we have $`a m = (a m_\lambda)_{\lambda \in \Lambda}`$, and each $a m_\lambda$ is in $\mathfrak{a} M_\lambda$.

$\prod \mathfrak{a} M_\lambda \subseteq \mathfrak{a}\left(\prod M_\lambda\right)$ : Let $\mathfrak{a} = (g_1, \ldots, g_n)$. Let $x = (x_\lambda) \in \prod \mathfrak{a} M_\lambda$. For each $\lambda$, $x_\lambda = \sum_{i=1}^n g_i m_{i,\lambda}$ for some $m_{i,\lambda} \in M_\lambda$. Let $`m^{(i)} = (m_{i,\lambda})_{\lambda \in \Lambda} \in \prod M_\lambda`$. Then $x = \sum_{i=1}^n g_i m^{(i)} \in \mathfrak{a}(\prod M_\lambda)$.

# 5.23
(Five Lemma) — Consider this commutative diagram:

$`\begin{CD}
M_4 @>\alpha_4>> M_3 @>\alpha_3>> M_2 @>\alpha_2>> M_1 @>\alpha_1>> M_0 \\
@V\gamma_4 VV @V\gamma_3 VV @V\gamma_2 VV @V\gamma_1 VV @V\gamma_0 VV \\
N_4 @>\beta_4>> N_3 @>\beta_3>> N_2 @>\beta_2>> N_1 @>\beta_1>> N_0
\end{CD}`$

Assume it has exact rows. Via a chase, prove these two statements:
1. If $\gamma_3$ and $\gamma_1$ are surjective and if $\gamma_0$ is injective, then $\gamma_2$ is surjective.
2. If $\gamma_3$ and $\gamma_1$ are injective and if $\gamma_4$ is surjective, then $\gamma_2$ is injective.

(_via a chase_ means essentially by definition - take an element and look at its images/preimages while using commutativity of the diagram).

**Proof**

1. Let $n_2 \in N_2$. Since $\gamma_1$ is surjective, there exists $m_1 \in M_1$ such that $\gamma_1(m_1) = \beta_2(n_2)$. By commutativity and exactness, $\gamma_0(\alpha_1(m_1)) = \beta_1(\gamma_1(m_1)) = \beta_1(\beta_2(n_2)) = 0$. Since $\gamma_0$ is injective, we have $\alpha_1(m_1) = 0$. By exactness of the top row, there exists $m_2 \in M_2$ such that $\alpha_2(m_2) = m_1$. Then by commutativity, $\beta_2(\gamma_2(m_2)) = \gamma_1(\alpha_2(m_2)) = \gamma_1(m_1) = \beta_2(n_2)$, so $\gamma_2(m_2) - n_2 \in \ker(\beta_2) = \mathrm{im}(\beta_3)$. Let $n_3 \in N_3$ such that $\beta_3(n_3) = \gamma_2(m_2) - n_2$. Since $\gamma_3$ is surjective, there exists $m_3 \in M_3$ such that $\gamma_3(m_3) = n_3$. Then $\gamma_2(\alpha_3(m_3)) = \beta_3(\gamma_3(m_3)) = \beta_3(n_3) = \gamma_2(m_2) - n_2$, so $\gamma_2(m_2 - \alpha_3(m_3)) = n_2$. Thus, $\gamma_2$ is surjective.
2. Let $m_2 \in M_2$ such that $\gamma_2(m_2) = 0$. Then $\beta_2(\gamma_2(m_2)) = 0$, so $\gamma_1(\alpha_2(m_2)) = 0$, so $\alpha_2(m_2) = 0$ since $\gamma_1$ is injective. By exactness of the top row, there exists $m_3 \in M_3$ such that $\alpha_3(m_3) = m_2$. Since $\beta_3(\gamma_3(m_3)) = \gamma_2(\alpha_3(m_3)) = \gamma_2(m_2) = 0$, we have $\gamma_3(m_3) \in \ker(\beta_3) = \mathrm{im}(\beta_4)$. Let $n_4 \in N_4$ such that $\beta_4(n_4) = \gamma_3(m_3)$. Since $\gamma_4$ is surjective, there exists $m_4 \in M_4$ such that $\gamma_4(m_4) = n_4$. Then $\gamma_3(\alpha_4(m_4)) = \beta_4(\gamma_4(m_4)) = \beta_4(n_4) = \gamma_3(m_3)$. Since $\gamma_3$ is injective, $m_3 = \alpha_4(m_4)\in \mathrm{im}(\alpha_4) = \ker(\alpha_3)$, so $m_2 = \alpha_3(m_3) = 0$. Thus, $\gamma_2$ is injective.

# Problem A.
Let $R_1$ and $R_2$ be two rings, and set $R=R_1 \times R_2$. Prove that any $R$-module $M$ is of the form $M_1 \times M_2 \simeq M$, where $M_{1,2}$ is an $R_{1,2}$-module, and the action of $R$ on $M_1 \times M_2$ is componentwise. (This can be proved directly, but consider using Problem 4.26.)

**Proof**

By Problem 4.26, it suffices to show that there exist homomorphisms $\alpha: M_1 \rightarrow M$, $\beta: M \rightarrow M_2$, $\sigma: M_2 \rightarrow M$, and $\rho: M \rightarrow M_1$ such that $\beta \alpha = 0$, $\beta \sigma = 1$, $\rho \sigma = 0$, $\rho \alpha = 1$, and $\alpha \rho + \sigma \beta = 1$. Define $`M_1 = \{m \in M \mid (0, r_2) \cdot m = 0 \text{ for all } r_2 \in R_2\}`$ and $`M_2 = \{m \in M \mid (r_1, 0) \cdot m = 0 \text{ for all } r_1 \in R_1\}`$. Let $\alpha(m_1) = m_1$ for $m_1 \in M_1$, $\beta(m) = (0,1) \cdot m$ for $m \in M$, $\sigma(m_2) = m_2$ for $m_2 \in M_2$, and $\rho(m) = (1,0) \cdot m$ for $m \in M$. We will verify the relations:

1. $\beta \alpha(m_1) = \beta(m_1) = 0$ for all $m_1 \in M_1$, so $\beta \alpha = 0$.
2. $\beta \sigma(m_2) = \beta(m_2) = m_2$ for all $m_2 \in M_2$, so $\beta \sigma = 1$.
3. $\rho \sigma(m_2) = \rho(m_2) = 0$ for all $m_2 \in M_2$, so $\rho \sigma = 0$.
4. $\rho \alpha(m_1) = \rho(m_1) = m_1$ for all $m_1 \in M_1$, so $\rho \alpha = 1$.
5. $\alpha \rho(m) + \sigma \beta(m) = (1,0) \cdot m + (0,1) \cdot m = m$ for all $m \in M$, so $\alpha \rho + \sigma \beta = 1$.

# Problem B.
Let $R$ be a ring, and consider the ring of formal power series $R[[t]]$. In class, we considered the case when $R$ is a field, and showed that in this case $R[[t]]$ is a local ring. Define a natural morphism $R[[t]] \rightarrow R$, and show that restriction along this morphism gives a bijection between the set of maximal ideals of $R$ and $R[[t]]$. (A special case of this is proved in the text.)
