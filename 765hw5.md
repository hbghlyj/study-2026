# 4.
Let $`U_0=\{[z, 1] \mid z \in \mathbb{C}\}`$ and $`U_1=\{[1, w] \mid w \in \mathbb{C}\}`$. Show that the vector bundle on $\mathbb{CP}^1$ with transition function

$\sigma_{01}(z)=\frac{z^n}{|z|^n}$

is isomorphic to $\mathscr{O}(-n)$.

**Proof**

The transition function $\sigma_{01}(z)=\frac{z^n}{|z|^n}$ has degree $n$ because its winding number is given by $\frac{1}{2\pi i}\int_{S^1} \sigma_{01}^{-1}d\sigma_{01} = \frac{1}{2\pi i}\int_{|z|=1} z^{-n}(nz^{n-1}dz) = \frac{n}{2\pi i}\int_{|z|=1} \frac{dz}{z} = n$. The first Chern class is the negative of the degree, so $c_1(L)=-n$. Since $c_1(\mathscr{O}(-n))=-n$, the bundle is isomorphic to $\mathscr{O}(-n)$.

# 5.
Consider $\mathscr{O}(-n)$ with the transition function above. Define connection forms

$A^{(0)}=n \frac{i \mathrm{Im}(\bar{z} dz)}{1+|z|^2}$

on $U_0$ and

$A^{(1)}=n \frac{i \mathrm{Im}(\bar{w} dw)}{1+|w|^2}$

on $U_1$.

(a) Show that these give a well-defined, metric-compatible connection $A$ on $\mathscr{O}(-n) \rightarrow \mathbb{C P}^1$.

To show the connection is well-defined, we verify that the connection forms satisfy the transformation rule $f^*A^{(1)} = A^{(0)} + g_{01}^{-1}dg_{01}$, where $f(z)=1/z$. For $\mathscr{O}(-n)$, the transition function is $g_{01} = (|z|/z)^n$.

*   **Pullback of $A^{(1)}$**: The pullback of $A^{(1)}$ to the $z$-chart is:
    $f^*A^{(1)} = n \frac{i \mathrm{Im}(\overline{(1/z)} d(1/z))}{1+|1/z|^2} = -n \frac{i \mathrm{Im}(\bar{z}dz)}{|z|^2(1+|z|^2)}$.

*   **Maurer-Cartan form**: The Maurer-Cartan form for $g_{01}$ is:
    $g_{01}^{-1}dg_{01} = d(n\log|z| - n\log z) = -n \frac{i \mathrm{Im}(\bar{z}dz)}{|z|^2}$.

*   **Verification**: We check if the sum of $A^{(0)}$ and the Maurer-Cartan form equals the pullback:
    $A^{(0)} + g_{01}^{-1}dg_{01} = n \frac{i \mathrm{Im}(\bar{z} dz)}{1+|z|^2} - n \frac{i \mathrm{Im}(\bar{z}dz)}{|z|^2} = -n \frac{i \mathrm{Im}(\bar{z}dz)}{|z|^2(1+|z|^2)}$.
    This matches $f^*A^{(1)}$, so the connection is well-defined.

(b) Compute its curvature.

(c) Use your answer from (b) to calculate the first Chern class of $\mathscr{O}(-n)$.

(d) Compare with HW3, problem 5.

# 6.
Check that the induced connection of a metric-compatible connection commutes with musical isomorphisms, i.e. $\nabla_X(s^\flat)=(\nabla_X s)^\flat$.

**Proof**

By definition, $s^{\flat} := g(s, -)$. Using the metric-compatibility of the connection, we have for any vector field $Y$:

$`\begin{align*}
(\nabla_X(s^\flat))(Y) &= X(s^\flat(Y)) - s^\flat(\nabla_X Y) \\
&= X(g(s, Y)) - g(s, \nabla_X Y) \\
&= g(\nabla_X s, Y) + g(s, \nabla_X Y) - g(s, \nabla_X Y) \\
&= g(\nabla_X s, Y) \\
&= ((\nabla_X s)^\flat)(Y)
\end{align*}`$

# 7.
Check that a connection satisfying

$\partial_i h_{\alpha \beta}=A_{i \alpha}^\gamma h_{\gamma \beta}+A_{i \beta}^\gamma h_{\alpha \gamma}$

in local coordinates is indeed metric-compatible.

**Proof**

$`\begin{align*}
\partial_i h_{\alpha \beta} &= A_{i \alpha}^\gamma h_{\gamma \beta}+A_{i \beta}^\gamma h_{\alpha \gamma} \\
&= \left< A_{i \alpha}^\gamma e_\gamma, e_\beta \right> + \left< e_\alpha, A_{i \beta}^\gamma e_\gamma \right> \\
&= \left< \nabla_{\frac{\partial}{\partial x^i}} e_\alpha, e_\beta \right> + \left< e_\alpha, \nabla_{\frac{\partial}{\partial x^i}} e_\beta \right>
\end{align*}`$

# 8.
Check that the Levi-Civita connection, given in local coordinates by the formula

$\Gamma_{i j}^k=\frac{1}{2} g^{k \ell}\left(\partial_i g_{j \ell}+\partial_j g_{i \ell}-\partial_{\ell} g_{i j}\right),$

is indeed compatible with $g$.

**Proof**

In a standard basis, metric compatibility reads

$`\begin{align*}
    \frac{\partial}{\partial x^i} \left< \frac{\partial}{\partial x^j},\frac{\partial}{\partial x^k} \right> &= \left< \nabla_{\frac{\partial}{\partial x^i}}\frac{\partial}{\partial x^j},\frac{\partial}{\partial x^k} \right>  + \left<\frac{\partial}{\partial x^j},\nabla_{\frac{\partial}{\partial x^i}}\frac{\partial}{\partial x^k} \right>.
\end{align*}`$

Express the right-hand side in terms of Christoffel symbols and the metric tensor

$`\begin{align*}
\left< \nabla_{\frac{\partial}{\partial x^i}}\frac{\partial}{\partial x^j},\frac{\partial}{\partial x^k} \right>  + \left<\frac{\partial}{\partial x^j},\nabla_{\frac{\partial}{\partial x^i}}\frac{\partial}{\partial x^k} \right> &= \Gamma_{ij}^l g_{lk} + \Gamma_{ik}^l g_{jl} \\
&= \frac{1}{2}(\partial_i g_{jk} + \partial_j g_{ik} - \partial_k g_{ij}) + \frac{1}{2}(\partial_i g_{kj} + \partial_k g_{ij} - \partial_j g_{ik}) \\
&= \frac{1}{2}(\partial_i g_{jk} + \partial_i g_{kj}) \\
&= \partial_i g_{jk}
\end{align*}`$
