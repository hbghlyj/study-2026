# 4.
Let $`U_0=\{[z, 1] \mid z \in \mathbb{C}\}`$ and $`U_1=\{[1, w] \mid w \in \mathbb{C}\}`$. Show that the vector bundle on $\mathbb{CP}^1$ with transition function

$\sigma_{01}(z)=\frac{z^n}{|z|^n}$

is isomorphic to $\mathscr{O}(-n)$.

**Proof**

The complex projective line $\mathbb{CP}^{1}$ is covered by two affine charts:

- $`U_{0}=\{[z:1]\}`$ with local coordinate $z$.

- $`U_{1}=\{[1:w]\}`$ with local coordinate $w$.

On their intersection $U_{0}\cap U_{1}$, the coordinates are related by $w=1/z$.

$\mathcal{O}(-1)$ is the tautological line bundle, where the fiber over a point $p\in \mathbb{CP}^{1}$ is the complex line in $\mathbb{C}^{2}$ represented by that point.

A point in the fiber over $U_{0}$ is written as $\zeta_{0}\frac{(z,1)}{\sqrt{|z|^2 + 1}}\in \mathbb{C}^{2}$ for some $`\zeta_{0}\in \mathbb{C}`$.

A point in the fiber over $U_{1}$ is written as $\zeta_{1}\frac{(1,w)}{\sqrt{|w|^2 + 1}}\in \mathbb{C}^{2}$ for some $\zeta_{1}\in \mathbb{C}$.

On the overlap, these two expressions must represent the same vector in $\mathbb{C}^{2}$:

$\zeta_{0}\frac{(z,1)}{\sqrt{|z|^2 + 1}}=\zeta_{1}\frac{(1,w)}{\sqrt{|w|^2 + 1}}$

Substitute $w=1/z$:

$\zeta_{0}\frac{(z,1)}{\sqrt{|z|^2 + 1}}=\zeta_{1}\frac{(1,1/z)}{\sqrt{|z|^{-2} + 1}}$

In the first component of the vector equation:

$\zeta_{0}\cdot\frac{z}{\sqrt{|z|^2 + 1}}=\zeta_{1}\cdot\frac1{\sqrt{|z|^{-2} + 1}}\implies \zeta_{0}=\frac{z^{-1}}{|z|^{-1}}\zeta _{1}$

so the transition function $\sigma_{01}(z)$ for $\mathcal{O}(-1)$ is $\frac{z^{-1}}{|z|^{-1}}$ under this choice of local basis. 

so the transition function for $\mathcal{O}(-n)$ is $\sigma_{01}(z)=\left(\frac{|z|}{z}\right)^n = \frac{|z|^n}{z^n}$.

# 5.
Consider $\mathscr{O}(-n)$ with the transition function above. Define connection forms

$A^{(0)}=n \frac{i \mathrm{Im}(\bar{z} dz)}{1+|z|^2}$

on $U_0$ and

$A^{(1)}=n \frac{i \mathrm{Im}(\bar{w} dw)}{1+|w|^2}$

on $U_1$.

(a) Show that these give a well-defined, metric-compatible connection $A$ on $\mathscr{O}(-n) \rightarrow \mathbb{C P}^1$.

**Proof**

For $\mathscr{O}(-n)$, by question 4, the transition function is $\sigma_{01} = (z/|z|)^n$.

On $U_0\cap U_1$, $w=1/z$,

$A^{(1)}=n \frac{i \mathrm{Im}(\bar{w} dw)}{1+|w|^2} = n \frac{i \mathrm{Im}(\overline{(1/z)} d(1/z))}{1+|1/z|^2} = -n \frac{i \mathrm{Im}(\bar{z}dz)}{|z|^2(1+|z|^2)}$.

$`\begin{align*}
\sigma_{01}^{-1}d\sigma_{01} &= \left(\frac{z}{|z|}\right)^{-n} d\left(\frac{z}{|z|}\right)^n \\
&= \left(\frac{z}{|z|}\right)^{-n} d\left(\frac{z}{\bar{z}}\right)^{\frac{n}2} \\
&= \left(\frac{z}{|z|}\right)^{-n} \frac{n}2\left(\frac{z}{\bar{z}}\right)^{\frac{n}2-1} d\left(\frac{z}{\bar{z}}\right) \\
&= \left(\frac{z}{|z|}\right)^{-n} \frac{n}2\left(\frac{z}{\bar{z}}\right)^{\frac{n}2-1} \frac{\bar{z}dz - zd\bar{z}}{\bar{z}^2} \\
&= n \frac{\bar{z}dz - zd\bar{z}}{2|z|^2} \\
&= n \frac{i \mathrm{Im}(\bar{z}dz)}{|z|^2}.
\end{align*}`$

To show the connection is well-defined, we verify that the connection forms satisfy the transformation rule $A^{(0)} = A^{(1)} + \sigma_{01}^{-1}d\sigma_{01}$

$A^{(0)} - \sigma_{01}^{-1}d\sigma_{01} = n \frac{i \mathrm{Im}(\bar{z} dz)}{1+|z|^2} - n \frac{i \mathrm{Im}(\bar{z}dz)}{|z|^2} = -n \frac{i \mathrm{Im}(\bar{z}dz)}{|z|^2(1+|z|^2)}$.

This matches $A^{(1)}$, so the connection is well-defined.

(b) Compute its curvature.

**Proof**

The curvature is $F=dA$. On $U_0$,

$`\begin{align*}
F = d A^{(0)} &= d\left(\frac{n}{2} \frac{\bar{z}dz - z d\bar{z}}{1+|z|^2}\right) \\
&= \frac{n}{2} \left( d\left(\frac{1}{1+|z|^2}\right) \wedge (\bar{z}dz - z d\bar{z}) + \frac{1}{1+|z|^2} d(\bar{z}dz - z d\bar{z}) \right) \\
&= \frac{n}{2} \left( -\frac{\bar{z}dz+z d\bar{z}}{(1+|z|^2)^2} \wedge (\bar{z}dz - z d\bar{z}) + \frac{-2 dz \wedge d\bar{z}}{1+|z|^2} \right) \\
&= \frac{n}{2} \left( \frac{2|z|^2 dz \wedge d\bar{z}}{(1+|z|^2)^2} + \frac{-2(1+|z|^2) dz \wedge d\bar{z}}{(1+|z|^2)^2} \right) \\
&= \frac{n}{2} \frac{-2 dz \wedge d\bar{z}}{(1+|z|^2)^2} \\
&= -n \frac{dz \wedge d\bar{z}}{(1+|z|^2)^2}.
\end{align*}`$

(c) Use your answer from (b) to calculate the first Chern class of $\mathscr{O}(-n)$.

**Proof**

The de Rham representative of the first Chern class is $c_1(\mathscr{O}(-n)) = \left[\frac{i}{2\pi}F\right]\in H^2_{\mathrm{dR}}(\mathbb{CP}^1)$.

Use $z=re^{i\theta}$, so $d\bar z\wedge dz = 2i\  r\ dr\wedge d\theta$. Then $F = n\frac{2i\  r}{(1+r^2)^2}\ dr\wedge d\theta$, and

$\frac{i}{2\pi}F = -n\frac{r}{\pi(1+r^2)^2}\ dr\wedge d\theta$.

Integrating over $\mathbb{CP}^1$ ( $r\in[0,\infty)$, $\theta\in[0,2\pi)$ ) gives

$\int_{\mathbb{CP}^1}\frac{i}{2\pi}F = -n\frac{1}{\pi}\int_0^{2\pi}\int_0^\infty \frac{r}{(1+r^2)^2}\ dr\ d\theta = -n\frac{2\pi}{\pi} \cdot \frac{1}{2} = -n,$

since $\int_0^\infty \frac{r}{(1+r^2)^2}\ dr = [-\frac1{2(1+r^2)}]_0^\infty=\frac{1}{2}$. Thus, $c_1(\mathscr{O}(-n)) = -n\in \mathbb{Z} \cong H^2(\mathbb{CP}^1;\mathbb{Z})$.

(d) Compare with HW3, problem 5.

# 6.
Check that the induced connection of a metric-compatible connection commutes with musical isomorphisms, i.e. $\nabla_X(s^\flat)=(\nabla_X s)^\flat$.

**Proof**

By definition, $s^{\flat} := g(s, -)$. Using the metric-compatibility of the connection, we have for any vector field $Y$:

$`\begin{align*}
(\nabla_X(s^\flat))(Y) &= X(s^\flat(Y)) - s^\flat(\nabla_X Y) \\
&= X(g(s, Y)) - g(s, \nabla_X Y) \\
&= g(\nabla_X s, Y) + g(s, \nabla_X Y) - g(s, \nabla_X Y) \\
&= g(\nabla_X s, Y) \\
&= ((\nabla_X s)^\flat)(Y)
\end{align*}`$

# 7.
Check that a connection satisfying

$\partial_i h_{\alpha \beta}=A_{i \alpha}^\gamma h_{\gamma \beta}+A_{i \beta}^\gamma h_{\alpha \gamma}$

in local coordinates is indeed metric-compatible.

**Proof**

$`\begin{align*}
\partial_i h_{\alpha \beta} &= A_{i \alpha}^\gamma h_{\gamma \beta}+A_{i \beta}^\gamma h_{\alpha \gamma} \\
&= \left< A_{i \alpha}^\gamma e_\gamma, e_\beta \right> + \left< e_\alpha, A_{i \beta}^\gamma e_\gamma \right> \\
&= \left< \nabla_{\frac{\partial}{\partial x^i}} e_\alpha, e_\beta \right> + \left< e_\alpha, \nabla_{\frac{\partial}{\partial x^i}} e_\beta \right>
\end{align*}`$

# 8.
Check that the Levi-Civita connection, given in local coordinates by the formula

$\Gamma_{i j}^k=\frac{1}{2} g^{k \ell}\left(\partial_i g_{j \ell}+\partial_j g_{i \ell}-\partial_{\ell} g_{i j}\right),$

is indeed compatible with $g$.

**Proof**

In a standard basis, metric compatibility reads

$`\begin{align*}
    \frac{\partial}{\partial x^i} \left< \frac{\partial}{\partial x^j},\frac{\partial}{\partial x^k} \right> &= \left< \nabla_{\frac{\partial}{\partial x^i}}\frac{\partial}{\partial x^j},\frac{\partial}{\partial x^k} \right>  + \left<\frac{\partial}{\partial x^j},\nabla_{\frac{\partial}{\partial x^i}}\frac{\partial}{\partial x^k} \right>.
\end{align*}`$

Express the right-hand side in terms of Christoffel symbols and the metric tensor

$`\begin{align*}
\left< \nabla_{\frac{\partial}{\partial x^i}}\frac{\partial}{\partial x^j},\frac{\partial}{\partial x^k} \right>  + \left<\frac{\partial}{\partial x^j},\nabla_{\frac{\partial}{\partial x^i}}\frac{\partial}{\partial x^k} \right> &= \Gamma_{ij}^l g_{lk} + \Gamma_{ik}^l g_{jl} \\
&= \frac{1}{2}(\partial_i g_{jk} + \partial_j g_{ik} - \partial_k g_{ij}) + \frac{1}{2}(\partial_i g_{kj} + \partial_k g_{ij} - \partial_j g_{ik}) \\
&= \frac{1}{2}(\partial_i g_{jk} + \partial_i g_{kj}) \\
&= \partial_i g_{jk}
\end{align*}`$
