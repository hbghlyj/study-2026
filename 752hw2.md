# 1.
Show that if $X$ is the union of contractible open subsets $A$ and $B$, then all cup products of positive-dimensional classes in $H^*(X)$ are zero. In particular, this is the case if $X$ is a suspension. Conclude that spaces such as $\mathbb{R P}^2$ and $T^2$ cannot be written as unions of two open contractible subsets.

**Proof**

Suppose $X = A \cup B$ with $A, B$ open and contractible.
Let $i_A^*: H^k(X) \to H^1(A)$ be the restriction map.
Since $A$ is contractible, $H^k(A) = 0$ for $k > 0$.
The long exact sequence of the pair $(X, A)$ gives us $H^k(X) \cong H^k(X, A)$ for $k > 0$.
Let $a\in H^k(X)$ and $b\in H^l(X)$ be positive-dimensional cohomology classes. Let $a' \in H^k(X, A)$ and $b' \in H^l(X, B)$ be the corresponding classes under the isomorphisms.
The absolute cup product $a \cup b$ corresponds to the relative cup product $a' \cup b' \in H^{k+l}(X, A \cup B)$. Since $A \cup B = X$, we have $H^{k+l}(X, A \cup B) = H^{k+l}(X, X) = 0$. By naturality, $a \cup b = 0$.

In $H^*(\mathbb{RP}^2)$ and in $`H^*(T^2)`$, the generator of $H^1$ cup with itself is nonzero. Therefore, $\mathbb{R P}^2$ and $T^2$ cannot be written as unions of two open contractible subsets.

# 2.
Is the Hopf map

$`f: S^3 \subset \mathbb{C}^2 \rightarrow S^2=\mathbb{C} \cup\{\infty\}, \quad(z, w) \mapsto \frac{z}{w}`$

nullhomotopic? Explain.

**Proof**

$[x,y,z]$ is the homogeneous coordinate of $\Bbb{CP}^2$. The 2-skeleton of $\Bbb{CP}^2$ is $\Bbb{CP}^1$, which can be identified with $S^2$ by $[x,y,0]\mapsto [x,y]$. The 4-cell is attached to the 2-skeleton by the map $S^3 \to S^2$ given by $(z,w)\mapsto [z,w]$, which is exactly the Hopf map.

The Hopf map is the attaching map of the $4$-cell of $`\Bbb{CP}^2`$ onto the 2-skeleton. It's not null-homotopic because if it were, by Proposition 0.18 in Hatcher, $`\Bbb{CP}^2`$ would be homotopy equivalent to $`S^2 \vee S^4`$, which can be shown to be false since the square of each element of $`H^2(S^2 \vee S^4; \mathbb{Z})`$ is zero, but the square of a generator of $`H^2(\Bbb{CP}^2; \mathbb{Z})`$ is nonzero since $`H^*(\Bbb{CP}^2; \mathbb{Z}) \cong \mathbb{Z}[\alpha]/(\alpha^3)`$, and $`H^4(\Bbb{CP}^2; \mathbb{Z})`$ is generated by $`\alpha^2`$.

# 3.
Is there a continuous map $f: X \rightarrow Y$ inducing isomorphisms on all of the cohomology groups (i.e., $f^*: H^i(Y ; \mathbb{Z}) \xrightarrow{\cong} H^i(X ; \mathbb{Z})$, for all $i$) but $X$ and $Y$ do not have isomorphic cohomology rings (with $\mathbb{Z}$ coefficients)? Explain your answer.

**Proof**

No. By Hatcher's Proposition 3.10, the map $`f^*: H^*(Y) \to H^*(X)`$ is a ring homomorphism with inverse ring homomorphism $`(f^*)^{-1}`$. So $`H^*(X)`$ and $`H^*(Y)`$ must have isomorphic cohomology rings. Therefore, no such map exists.

# 4.
Show that $\mathbb{RP}^3$ and $\mathbb{RP}^2 \vee S^3$ have the same cohomology rings with integer coefficients.

**Proof**

We first compute the cohomology groups with $\mathbb{Z}$ coefficients.

For $X = \mathbb{RP}^3$, the homology groups are $H_0=\mathbb{Z}, H_1=\mathbb{Z}/2, H_2=0, H_3=\mathbb{Z}$. By the Universal Coefficient Theorem, the cohomology groups are $H^0=\mathbb{Z}, H^1=0, H^2=\mathbb{Z}/2, H^3=\mathbb{Z}$.

For $Y = \mathbb{RP}^2 \vee S^3$, the homology groups are the same, and thus the cohomology groups are also the same.

So $`H^*(\mathbb{RP}^3; \mathbb{Z}) \cong H^*(\mathbb{RP}^2 \vee S^3; \mathbb{Z})`$ as graded abelian groups.

For the ring structure, we consider cup products of positive-dimensional classes.

For $Y = \mathbb{RP}^2 \vee S^3$, the reduced cohomology is $`\tilde{H}^*(Y) \cong \tilde{H}^*(\mathbb{RP}^2) \oplus \tilde{H}^*(\mathbb{S}^3)`$. The cup product of any two elements from $\tilde{H}^*(Y)$ is zero.

For $X = \mathbb{RP}^3$, let $\alpha \in H^2(X; \mathbb{Z})$ be the generator. The only potentially non-trivial product is $\alpha \cup \alpha \in H^4(X; \mathbb{Z})$. But $H^4(X; \mathbb{Z}) = 0$, so $\alpha \cup \alpha = 0$.

Thus, both rings have trivial multiplication for positive-dimensional classes.

Therefore, the cohomology rings are isomorphic.

# 5.
(a) Show that $H^*\left(\mathbb{C P}^n ; \mathbb{Z}\right) \cong \mathbb{Z}[x] /\left(x^{n+1}\right)$, with $x$ the generator of $H^2\left(\mathbb{C P}^n ; \mathbb{Z}\right)$.

**Proof**

$$H^*\left(\mathbb{C P}^n ; \mathbb{Z}\right) \cong \mathbb{Z}[x] /\left(x^{n+1}\right)$$

The CW structure of $\mathbb{C P}^n$ consists of one cell in each even dimension $0, 2, 4, \ldots, 2n$. The cohomology groups are $H^{2k}(\mathbb{C P}^n; \mathbb{Z}) \cong \mathbb{Z}$ for $0 \leq k \leq n$ and $H^m(\mathbb{C P}^n; \mathbb{Z}) = 0$ for odd $m$ or $m > 2n$.

(b) Show that the Lefschetz number $\tau_f$ of a map $f: \mathbb{C P}^n \rightarrow \mathbb{C P}^n$ is given by

$\tau_f=1+d+d^2+\cdots+d^n,$

where $f^*(x)=d x$ for some $d \in \mathbb{Z}$, and with $x$ as in part (a).

**Proof**

$`f_*: H_i(X) \rightarrow H_i(X)`$ is given by $`f_*(\alpha) = d^k \alpha`$ for $`\alpha \in H_{2k}(\mathbb{C P}^n; \mathbb{Z})`$.

(c) Show that for $n$ even, any map $f: \mathbb{C P}^n \rightarrow \mathbb{C P}^n$ has a fixed point.

**Proof**

When $n$ is even, the Lefschetz number $\tau_f$ is given by $1 + d + d^2 + \cdots + d^n$. This is a geometric series with $n+1$ terms. The sum of this series can be expressed as:
$\tau_f = \frac{d^{n+1} - 1}{d - 1}$ for $d \neq 1$, and $\tau_f = n + 1$ for $d = 1$.

Therefore,
- If $d = 1$, then $\tau_f = n + 1$, which is positive.
- If $d \neq 1$, then $\tau_f = \frac{d^{n+1} - 1}{d - 1}$. Since $n$ is even, $d^{n+1} - 1$ will be negative for $d < 1$ and positive for $d > 1$. However, the denominator $d - 1$ will also change sign accordingly, ensuring that $\tau_f$ is always positive.

In either case, $\tau_f > 0$, which implies that $f$ has a fixed point by the Lefschetz Fixed Point Theorem.

(d) When $n$ is odd, show that there is a fixed point unless $f^*(x)=-x$, where $x$ denotes as before a generator of $H^2\left(\mathbb{C P}^n ; \mathbb{Z}\right)$.
# 6.
Use cup products to compute the map $`H^*\left(\mathbb{C P}^n ; \mathbb{Z}\right) \rightarrow H^*\left(\mathbb{C P}^n ; \mathbb{Z}\right)`$ induced by the map $\mathbb{C P}^n \rightarrow \mathbb{C P}^n$ that is a quotient of the map $\mathbb{C}^{n+1} \rightarrow \mathbb{C}^{n+1}$ raising each coordinate to the $d$-th power, $\left(z_0, \cdots, z_n\right) \mapsto\left(z_0^d, \cdots, z_n^d\right)$, for a fixed integer $d>0$.

(Hint: First do the case $n=1$.)

**Proof**

For $n=1$, $\mathbb{C P}^1$ is homeomorphic to $S^2$. The map $\mathbb{C P}^1 \to \mathbb{C P}^1$ given by $[z_0, z_1] \mapsto [z_0^d, z_1^d]$ corresponds to the map $S^2 \to S^2$ of degree $d$. The induced map on cohomology sends the generator of $H^2(S^2; \mathbb{Z})$ to $d$ times itself.

For $n > 1$, the 2-skeleton of $\mathbb{C P}^n$ is $\mathbb{C P}^1$, and the map restricts to the degree $d$ map on this skeleton. Since the cohomology ring of $\mathbb{C P}^n$ is generated by the degree 2 class, the induced map on cohomology sends the generator $x$ of $H^2(\mathbb{C P}^n; \mathbb{Z})$ to $d x$. Therefore, the induced map on cohomology is given by $x \mapsto d x$, and thus $`H^*(\mathbb{C P}^n; \mathbb{Z}) \to H^*(\mathbb{C P}^n; \mathbb{Z})`$ sends $x^k$ to $d^k x^k$ for all $k$.
# 7.
Describe the cohomology ring $`H^*(X \vee Y)`$ of a wedge sum of two spaces.

**Proof**

The cohomology groups of a wedge sum of (well-pointed) spaces are given by $`\tilde{H}^n(X \vee Y) \cong \tilde{H}^n(X) \oplus \tilde{H}^n(Y)`$ for reduced cohomology. For unreduced cohomology, $`H^n(X \vee Y) \cong H^n(X) \oplus H^n(Y)`$ for $`n>0`$, while $`H^0(X \vee Y) \cong \mathbb{Z}`$ if $`X, Y`$ are path-connected.

To determine the ring structure, we examine the cup product. Let $`u \in \tilde{H}^p(X)`$ and $`v \in \tilde{H}^q(Y)`$. We consider these as classes in $`\tilde{H}^*(X \vee Y)`$ via the maps induced by the projections $`p_X: X \vee Y \to X`$ (collapsing $Y$ to the basepoint) and $`p_Y: X \vee Y \to Y`$ (collapsing $X$ to the basepoint). Let $`\alpha = p_X^*(u)`$ and $`\beta = p_Y^*(v)`$ be these classes in $`\tilde{H}^*(X \vee Y)`$. We want to show that $`\alpha \cup \beta = 0`$.

Consider the inclusion maps $`i_X: X \to X \vee Y`$ and $`i_Y: Y \to X \vee Y`$. These induce maps $`i_X^*: \tilde{H}^*(X \vee Y) \to \tilde{H}^*(X)`$ and $`i_Y^*: \tilde{H}^*(X \vee Y) \to \tilde{H}^*(Y)`$. The map $`\Phi = (i_X^*, i_Y^*): \tilde{H}^*(X \vee Y) \to \tilde{H}^*(X) \oplus \tilde{H}^*(Y)`$ is an isomorphism. If we can show that $`i_X^*(\alpha \cup \beta) = 0`$ and $`i_Y^*(\alpha \cup \beta) = 0`$, then it follows that $`\alpha \cup \beta = 0`$.

By the naturality of the cup product, $`i_X^*(\alpha \cup \beta) = i_X^*(\alpha) \cup i_X^*(\beta)`$.
Let's evaluate the terms:
1. $`i_X^*(\alpha) = i_X^*(p_X^*(u))`$. Since $`p_X \circ i_X = id_X`$, we have $`i_X^*(p_X^*(u)) = (p_X \circ i_X)^*(u) = id_X^*(u) = u`$.
2. $`i_X^*(\beta) = i_X^*(p_Y^*(v))`$. Since $`p_Y \circ i_X: X \to Y`$ is the constant map to the basepoint (as $p_Y$ collapses $X$ to the basepoint), the induced map $`(p_Y \circ i_X)^*: \tilde{H}^*(Y) \to \tilde{H}^*(X)`$ is the zero map. Thus, $`i_X^*(p_Y^*(v)) = (p_Y \circ i_X)^*(v) = 0`$.
Therefore, $`i_X^*(\alpha \cup \beta) = u \cup 0 = 0`$.

Similarly, by naturality of the cup product, $`i_Y^*(\alpha \cup \beta) = i_Y^*(\alpha) \cup i_Y^*(\beta)`$.
Let's evaluate the terms:
1. $`i_Y^*(\alpha) = i_Y^*(p_X^*(u))`$. Since $`p_X \circ i_Y: Y \to X`$ is the constant map to the basepoint (as $p_X$ collapses $Y$ to the basepoint), the induced map $`(p_X \circ i_Y)^*: \tilde{H}^*(X) \to \tilde{H}^*(Y)`$ is the zero map. Thus, $`i_Y^*(p_X^*(u)) = (p_X \circ i_Y)^*(u) = 0`$.
2. $`i_Y^*(\beta) = i_Y^*(p_Y^*(v))`$. Since $`p_Y \circ i_Y = id_Y`$, we have $`i_Y^*(p_Y^*(v)) = (p_Y \circ i_Y)^*(v) = id_Y^*(v) = v`$.
Therefore, $`i_Y^*(\alpha \cup \beta) = 0 \cup v = 0`$.

Since both components of $`\Phi(\alpha \cup \beta)`$ are zero, and $`\Phi`$ is an isomorphism, it must be that $`\alpha \cup \beta = 0`$ in $`\tilde{H}^*(X \vee Y)`$.
Thus, the cup product of cohomology classes from different summands in a wedge sum is zero, and the cohomology ring of $`X \vee Y`$ is isomorphic to the direct sum of the cohomology rings of $`X`$ and $`Y`$, with the understanding that the degree-0 components are identified and products between elements from the different rings' reduced cohomology are zero.

# 8.
Let $\mathbb{H}=\mathbb{R} \cdot 1 \oplus \mathbb{R} \cdot i \oplus \mathbb{R} \cdot j \oplus \mathbb{R} \cdot k$ be the skew-field of quaternions, where $i^2=j^2=k^2=-1$ and $i j=k=-j i, j k=i=-k j, k i=j=-i k$. For a quaternion $q=a+b i+c j+d k, a, b, c, d \in \mathbb{R}$, its conjugate is defined by $\bar{q}=a-b i-c j-d k$. Let $|q|:=\sqrt{a^2+b^2+c^2+d^2}$.

(a) Verify the following formulae in $\mathbb{H}: q \cdot \bar{q}=|q|^2, \overline{q_1 q_2}=\bar{q}_2 \bar{q}_1,\left|q_1 q_2\right|=\left|q_1\right| \cdot\left|q_2\right|$.

**Proof**

For $`q=a+b i+c j+d k`$, we have $`q \cdot \bar{q}=(a+b i+c j+d k)(a-b i-c j-d k)=a^2+b^2+c^2+d^2=|q|^2`$.

For $`\overline{q_1 q_2}`$, the property $`\overline{q_1 q_2}=\bar{q}_2 \bar{q}_1`$ can be verified by expanding both sides and comparing terms.

For $`\left|q_1 q_2\right|`$, we have:

$`\begin{aligned}
\left|q_1 q_2\right| &= \sqrt{q_1 q_2 \cdot \overline{q_1 q_2}} \\
&= \sqrt{q_1 q_2 \cdot \bar{q}_2 \bar{q}_1} \\
&= \sqrt{q_1 \cdot\left(q_2 \cdot \bar{q}_2\right) \cdot \bar{q}_1} \\
&= \sqrt{q_1 \cdot\left|q_2\right|^2 \cdot \bar{q}_1} \\
&= \sqrt{\left|q_2\right|^2 \cdot\left(q_1 \cdot \bar{q}_1\right)} \\
&= \sqrt{\left|q_2\right|^2 \cdot\left|q_1\right|^2} \\
&= \left|q_1\right| \cdot\left|q_2\right|
\end{aligned}`$

(b) Let $S^7 \subset \mathbb{H} \oplus \mathbb{H}$ be the unit sphere, and let $`f: S^7 \rightarrow S^4=\mathbb{HP}^1=\mathbb{H} \cup\{\infty\}`$ be given by $f\left(q_1, q_2\right)=q_1 q_2{ }^{-1}$. Show that for any $p \in S^4$, the fiber $f^{-1}(p)$ is homeomorphic to $S^3$.

**Proof**

For the fiber over the point at infinity (corresponding to $`q_2 = 0`$), it consists of points $`(q_1, 0)`$ with $`|q_1| = 1`$, which is the set of unit quaternions, homeomorphic to $`S^3`$.

For a point corresponding to finite $p \in \mathbb{H}$, fix $(q_1, q_2) \in f^{-1}(p)$ with $q_2 \neq 0$. Any other point $(q_1', q_2') \in f^{-1}(p)$ can be expressed uniquely as $(q_1 u, q_2 u)$ where $u = q_2^{-1} q_2'$ is the unique unit quaternion corresponding to the point $(q_1', q_2')$. Thus, the fiber is the orbit of $(q_1, q_2)$ under the faithful action of unit quaternions and is therefore homeomorphic to $S^3$.

(c) Let $\mathbb{H P}^n$ be the quaternionic projective space defined exactly as in the complex case as the quotient of $`\mathbb{H}^{n+1} \setminus\{0\}`$ by the equivalence relation $v \sim \lambda v$, for $`\lambda \in \mathbb{H} \setminus\{0\}`$. Show that the CW structure of $\mathbb{H P}^n$ consists of only one cell in each dimension $0,4,8, \cdots, 4 n$, and calculate the homology of $\mathbb{H P}^n$.

**Proof**

It has exactly one cell in each dimension $4k$ for $0 \leq k \leq n$. Inductively, $\mathbb{H}P^1 \cong S^4 = e^0 \cup e^4$, and for $n > 1$, $\mathbb{H}P^n$ is obtained by attaching a cell of dimension $4n$ to $\mathbb{H}P^{n-1}$ along the attaching map $S^{4n-1} \to \mathbb{H}P^{n-1}$ given by the generalized quaternionic Hopf fibration.

The cellular chain complex $C_*(\mathbb{H P}^n)$ has $C_{4k}(\mathbb{H P}^n) \cong \mathbb{Z}$ for $0 \leq k \leq n$ and is 0 otherwise. Since all non-trivial chain groups are separated by 3 zero groups, all boundary maps $d_i: C_i \to C_{i-1}$ must be zero. Therefore, the homology groups are isomorphic to the chain groups:

$`H_i(\mathbb{H P}^n; \mathbb{Z}) \cong \begin{cases} \mathbb{Z} & \text{if } i=4k \text{ for } 0 \leq k \leq n \\ 0 & \text{otherwise} \end{cases}`$

(d) Show that $H^*\left(\mathbb{H P}^n ; \mathbb{Z}\right) \cong \mathbb{Z}[x] /\left(x^{n+1}\right)$, with $x$ the generator of $H^4\left(\mathbb{H P}^n ; \mathbb{Z}\right)$.

(e) Show that $S^4 \vee S^8$ and $\mathbb{H P}^2$ are not homotopy equivalent.
# 9.
For a map $f: S^{2 n-1} \rightarrow S^n$ with $n \geq 2$, let $X_f=S^n \cup_f D^{2 n}$ be the CW complex obtained by attaching a $2 n$-cell to $S^n$ by the map $f$. Let $a \in H^n\left(X_f ; \mathbb{Z}\right)$ and $b \in H^{2 n}\left(X_f ; \mathbb{Z}\right)$ be the generators of respective groups. The Hopf invariant $H(f) \in \mathbb{Z}$ of the map $f$ is defined by the identity $a^2=H(f) b$.

(a) Let $`f: S^3 \rightarrow S^2=\mathbb{C} \cup\{\infty\}`$ be given by $f\left(z_1, z_2\right)=z_1 / z_2$, for $\left(z_1, z_2\right) \in S^3 \subset \mathbb{C}^2$. Show that $X_f=\mathbb{C P}^2$ and $H(f)= \pm 1$.

(b) Let $`f: S^7 \rightarrow S^4=\mathbb{H} \cup\{\infty\}`$ be given by $f\left(q_1, q_2\right)=q_1 q_2{ }^{-1}$ in terms of quaternions $\left(q_1, q_2\right) \in S^7$, the unit sphere in $\mathbb{H}^2$. Show that $X_f=\mathbb{HP}^2$ and $H(f)= \pm 1$.
